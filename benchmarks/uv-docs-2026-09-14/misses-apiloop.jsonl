{"id": "q0001", "q": "Why is it best practice to declare dependencies in a static file instead of installing them ad hoc?", "shape": "conceptual", "gold": ["pip/dependencies.md#0"], "gold_text": ["# Declaring dependencies\n\nIt is best practice to declare dependencies in a static file instead of modifying environments with\nad-hoc installations. Once dependencies are defined, they can be [locked](./compile.md) to create a\nconsistent, reproducible environment."], "retrieved": []}
{"id": "q0002", "q": "How do you remove outdated cache entries and centralized project environments in uv?", "shape": "conceptual", "gold": ["getting-started/features.md#6"], "gold_text": ["## Utility\n\nManaging and inspecting uv's state, such as the cache, storage directories, or performing a\nself-update:\n\n- `uv cache clean`: Remove cache entries.\n- `uv cache prune`: Remove outdated cache entries and all centralized project environments.\n- `uv cache dir`: Show the uv cache directory path.\n- `uv tool dir`: Show the uv tool directory path.\n- `uv python dir`: Show the uv installed Pytho"], "retrieved": ["concepts/cache.md#1"]}
{"id": "q0003", "q": "What does `uv python dir` show?", "shape": "exact_lookup", "gold": ["getting-started/features.md#6"], "gold_text": ["## Utility\n\nManaging and inspecting uv's state, such as the cache, storage directories, or performing a\nself-update:\n\n- `uv cache clean`: Remove cache entries.\n- `uv cache prune`: Remove outdated cache entries and all centralized project environments.\n- `uv cache dir`: Show the uv cache directory path.\n- `uv tool dir`: Show the uv tool directory path.\n- `uv python dir`: Show the uv installed Pytho"], "retrieved": []}
{"id": "q0004", "q": "When would a Python project usually need packaging instead of staying unbuilt?", "shape": "conceptual", "gold": ["concepts/projects/config.md#6"], "gold_text": ["## Project packaging\n\nAs discussed in [build systems](#build-systems), a Python project must be built to be installed.\nThis process is generally referred to as \"packaging\".\n\nYou probably need a package if you want to:\n\n- Add commands to the project\n- Distribute the project to others\n- Use a `src` and `test` layout\n- Write a library\n\nYou probably _do not_ need a package if you are:\n\n- Writing scrip"], "retrieved": []}
{"id": "q0005", "q": "What does `tool.uv.package` control in a project?", "shape": "exact_lookup", "gold": ["concepts/projects/config.md#6"], "gold_text": ["## Project packaging\n\nAs discussed in [build systems](#build-systems), a Python project must be built to be installed.\nThis process is generally referred to as \"packaging\".\n\nYou probably need a package if you want to:\n\n- Add commands to the project\n- Distribute the project to others\n- Use a `src` and `test` layout\n- Write a library\n\nYou probably _do not_ need a package if you are:\n\n- Writing scrip"], "retrieved": ["concepts/projects/config.md#1"]}
{"id": "q0006", "q": "How do you create a locked requirements file using pip freeze?", "shape": "conceptual", "gold": ["guides/migration/pip-to-project.md#3"], "gold_text": ["Though less common, the `requirements.txt` can also be generated using `pip freeze`, by first\ninstalling the input dependencies into the environment then exporting the installed versions:\n\n```console\n$ pip install -r requirements.in\n$ pip freeze > requirements.txt\n```\n\n```requirements title=\"requirements.txt\"\nannotated-types==0.7.0\nanyio==4.8.0\nfastapi==0.115.11\nidna==3.10\npydantic==2.10.6\npydanti"], "retrieved": []}
{"id": "q0007", "q": "What does pip freeze do when generating requirements.txt?", "shape": "exact_lookup", "gold": ["guides/migration/pip-to-project.md#3"], "gold_text": ["Though less common, the `requirements.txt` can also be generated using `pip freeze`, by first\ninstalling the input dependencies into the environment then exporting the installed versions:\n\n```console\n$ pip install -r requirements.in\n$ pip freeze > requirements.txt\n```\n\n```requirements title=\"requirements.txt\"\nannotated-types==0.7.0\nanyio==4.8.0\nfastapi==0.115.11\nidna==3.10\npydantic==2.10.6\npydanti"], "retrieved": ["pip/inspection.md#3"]}
{"id": "q0010", "q": "Why are lower bounds important when dependency versions conflict?", "shape": "conceptual", "gold": ["concepts/resolution.md#25"], "gold_text": ["## Lower bounds\n\nBy default, `uv add` adds lower bounds to dependencies and, when using uv to manage projects, uv\nwill warn if direct dependencies don't have lower bounds.\n\nLower bounds are not critical in the \"happy path\", but they are important for cases where there are\ndependency conflicts. For example, consider a project that requires two packages and those packages\nhave conflicting dependenci"], "retrieved": ["concepts/resolution.md#?"]}
{"id": "q0011", "q": "What does `--resolution lowest` do when testing dependency bounds?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#25"], "gold_text": ["## Lower bounds\n\nBy default, `uv add` adds lower bounds to dependencies and, when using uv to manage projects, uv\nwill warn if direct dependencies don't have lower bounds.\n\nLower bounds are not critical in the \"happy path\", but they are important for cases where there are\ndependency conflicts. For example, consider a project that requires two packages and those packages\nhave conflicting dependenci"], "retrieved": ["concepts/resolution.md#1"]}
{"id": "q0012", "q": "How can you run a marimo notebook as a script without opening the browser?", "shape": "conceptual", "gold": ["guides/integration/marimo.md#5"], "gold_text": ["## Running marimo notebooks as scripts\n\nRegardless of how your dependencies are managed (with inline script metadata, within a project, or\nwith a non-project environment), you can run marimo notebooks as scripts with:\n\n```console\n$ uv run my_notebook.py\n```\n\nThis executes your notebook as a Python script, without opening an interactive session in your\nbrowser."], "retrieved": ["guides/integration/marimo.md#1"]}
{"id": "q0013", "q": "Why might uv reject a package that pip would install?", "shape": "conceptual", "gold": ["pip/compatibility.md#16"], "gold_text": ["## Strictness and spec enforcement\n\nuv tends to be stricter than `pip`, and will often reject packages that `pip` would install. For\nexample, uv rejects HTML indexes with invalid URL fragments (see:\n[PEP 503](https://peps.python.org/pep-0503/)), while `pip` will ignore such fragments.\n\nIn some cases, uv implements lenient behavior for popular packages that are known to have specific\nspec complianc"], "retrieved": ["pip/compatibility.md#1"]}
{"id": "q0014", "q": "How can uv run load environment variables from dotenv files, and which source wins if a variable is defined both places?", "shape": "conceptual", "gold": ["concepts/configuration-files.md#3"], "gold_text": ["## Environment variable files\n\n`uv run` can load environment variables from dotenv files (e.g., `.env`, `.env.local`,\n`.env.development`), powered by the [`dotenvy`](https://github.com/allan2/dotenvy) crate.\n\nTo load a `.env` file from a dedicated location, set the `UV_ENV_FILE` environment variable, or pass\nthe `--env-file` flag to `uv run`.\n\nFor example, to load environment variables from a `.en"], "retrieved": []}
{"id": "q0015", "q": "What does the UV_NO_ENV_FILE environment variable do for uv run?", "shape": "exact_lookup", "gold": ["concepts/configuration-files.md#3"], "gold_text": ["## Environment variable files\n\n`uv run` can load environment variables from dotenv files (e.g., `.env`, `.env.local`,\n`.env.development`), powered by the [`dotenvy`](https://github.com/allan2/dotenvy) crate.\n\nTo load a `.env` file from a dedicated location, set the `UV_ENV_FILE` environment variable, or pass\nthe `--env-file` flag to `uv run`.\n\nFor example, to load environment variables from a `.en"], "retrieved": ["guides/scripts.md#1"]}
{"id": "q0016", "q": "How does a tool environment choose its Python version, and which local version requests does it ignore?", "shape": "conceptual", "gold": ["concepts/tools.md#8"], "gold_text": ["## Python versions\n\nEach tool environment is linked to a specific Python version. This uses the same Python version\n[discovery logic](./python-versions.md#discovery-of-python-versions) as other virtual environments\ncreated by uv, but will ignore non-global Python version requests like `.python-version` files and\nthe `requires-python` value from a `pyproject.toml`.\n\nThe `--python` option can be use"], "retrieved": []}
{"id": "q0017", "q": "What does the --python option do for a tool environment?", "shape": "exact_lookup", "gold": ["concepts/tools.md#8"], "gold_text": ["## Python versions\n\nEach tool environment is linked to a specific Python version. This uses the same Python version\n[discovery logic](./python-versions.md#discovery-of-python-versions) as other virtual environments\ncreated by uv, but will ignore non-global Python version requests like `.python-version` files and\nthe `requires-python` value from a `pyproject.toml`.\n\nThe `--python` option can be use"], "retrieved": []}
{"id": "q0019", "q": "What does `--with jupyter` do in `uv run --with jupyter jupyter lab`?", "shape": "exact_lookup", "gold": ["guides/integration/jupyter.md#1"], "gold_text": ["## Using Jupyter within a project\n\nIf you're working within a [project](../../concepts/projects/index.md), you can start a Jupyter\nserver with access to the project's virtual environment via the following:\n\n```console\n$ uv run --with jupyter jupyter lab\n```\n\nBy default, `jupyter lab` will start the server at\n[http://localhost:8888/lab](http://localhost:8888/lab).\n\nWithin a notebook, you can import"], "retrieved": []}
{"id": "q0020", "q": "How does uv handle packages that don't have source distributions for every platform or Python version?", "shape": "conceptual", "gold": ["concepts/resolution.md#8"], "gold_text": ["## Required environments\n\nIn the Python ecosystem, packages can be published as source distributions, built distributions\n(wheels), or both; but to install a package, a built distribution is required. If a package lacks a\nbuilt distribution, or lacks a distribution for the current platform or Python version (built\ndistributions are often platform-specific), uv will attempt to build the package fro"], "retrieved": []}
{"id": "q0021", "q": "What does the required-environments setting control in uv resolution?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#8"], "gold_text": ["## Required environments\n\nIn the Python ecosystem, packages can be published as source distributions, built distributions\n(wheels), or both; but to install a package, a built distribution is required. If a package lacks a\nbuilt distribution, or lacks a distribution for the current platform or Python version (built\ndistributions are often platform-specific), uv will attempt to build the package fro"], "retrieved": []}
{"id": "q0022", "q": "How does uv handle legacy setuptools scripts on Windows when you omit the file extension?", "shape": "conceptual", "gold": ["concepts/projects/run.md#3"], "gold_text": ["## Legacy scripts on Windows\n\nSupport is provided for\n[legacy setuptools scripts](https://packaging.python.org/en/latest/guides/distributing-packages-using-setuptools/#scripts).\nThese types of scripts are additional files installed by setuptools in `.venv\\Scripts`.\n\nCurrently only legacy scripts with the `.ps1`, `.cmd`, and `.bat` extensions are supported.\n\nFor example, below is an example running"], "retrieved": []}
{"id": "q0023", "q": "What does the `.ps1` extension mean for legacy Windows scripts in uv?", "shape": "exact_lookup", "gold": ["concepts/projects/run.md#3"], "gold_text": ["## Legacy scripts on Windows\n\nSupport is provided for\n[legacy setuptools scripts](https://packaging.python.org/en/latest/guides/distributing-packages-using-setuptools/#scripts).\nThese types of scripts are additional files installed by setuptools in `.venv\\Scripts`.\n\nCurrently only legacy scripts with the `.ps1`, `.cmd`, and `.bat` extensions are supported.\n\nFor example, below is an example running"], "retrieved": []}
{"id": "q0024", "q": "How do you force only a specific package to come from a private package index while leaving all others on PyPI?", "shape": "conceptual", "gold": ["concepts/indexes.md#2"], "gold_text": ["## Pinning a package to an index\n\nA package can be pinned to a specific index by specifying the index in its `tool.uv.sources` entry.\nFor example, to ensure that `torch` is _always_ installed from the `pytorch` index, add the\nfollowing to your `pyproject.toml`:\n\n```toml\n[tool.uv.sources]\ntorch = { index = \"pytorch\" }\n\n[[tool.uv.index]]\nname = \"pytorch\"\nurl = \"https://download.pytorch.org/whl/cpu\"\n"], "retrieved": []}
{"id": "q0025", "q": "What does `explicit = true` do for an index?", "shape": "exact_lookup", "gold": ["concepts/indexes.md#2"], "gold_text": ["## Pinning a package to an index\n\nA package can be pinned to a specific index by specifying the index in its `tool.uv.sources` entry.\nFor example, to ensure that `torch` is _always_ installed from the `pytorch` index, add the\nfollowing to your `pyproject.toml`:\n\n```toml\n[tool.uv.sources]\ntorch = { index = \"pytorch\" }\n\n[[tool.uv.index]]\nname = \"pytorch\"\nurl = \"https://download.pytorch.org/whl/cpu\"\n"], "retrieved": ["concepts/indexes.md#1"]}
{"id": "q0026", "q": "How do you include optional dependency extras when running uv sync?", "shape": "conceptual", "gold": ["concepts/projects/sync.md#7"], "gold_text": ["### Syncing optional dependencies\n\nuv reads optional dependencies from the `[project.optional-dependencies]` table. These are\nfrequently referred to as \"extras\".\n\nuv does not sync extras by default. Use the `--extra` option to include an extra.\n\n```console\n$ uv sync --extra foo\n```\n\nTo quickly enable all extras, use the `--all-extras` option.\n\nSee the [optional dependencies](./dependencies.md#opti"], "retrieved": []}
{"id": "q0027", "q": "What does the `--all-extras` option do in uv sync?", "shape": "exact_lookup", "gold": ["concepts/projects/sync.md#7"], "gold_text": ["### Syncing optional dependencies\n\nuv reads optional dependencies from the `[project.optional-dependencies]` table. These are\nfrequently referred to as \"extras\".\n\nuv does not sync extras by default. Use the `--extra` option to include an extra.\n\n```console\n$ uv sync --extra foo\n```\n\nTo quickly enable all extras, use the `--all-extras` option.\n\nSee the [optional dependencies](./dependencies.md#opti"], "retrieved": []}
{"id": "q0028", "q": "Why is it recommended to regularly update dependencies?", "shape": "conceptual", "gold": ["guides/integration/renovate.md#0"], "gold_text": ["# Renovate\n\nIt is considered best practice to regularly update dependencies, to avoid being exposed to\nvulnerabilities, limit incompatibilities between dependencies, and avoid complex upgrades when\nupgrading from a too old version.\n\nuv is supported by [Renovate](https://github.com/renovatebot/renovate)."], "retrieved": []}
{"id": "q0029", "q": "How does uv.lock differ from requirements.txt when managing dependencies across platforms?", "shape": "conceptual", "gold": ["guides/migration/pip-to-project.md#8"], "gold_text": ["### The uv lockfile\n\nuv uses a lockfile (`uv.lock`) file to lock package versions. The format of this file is specific to\nuv, allowing uv to support advanced features. It replaces `requirements.txt` files.\n\nThe lockfile will be automatically created and populated when adding dependencies, but you can\nexplicitly create it with `uv lock`.\n\nUnlike `requirements.txt` files, the `uv.lock` file can repr"], "retrieved": []}
{"id": "q0031", "q": "What does the --bounds flag change when adding a dependency?", "shape": "exact_lookup", "gold": ["concepts/projects/dependencies.md#1"], "gold_text": ["## Adding dependencies\n\nTo add a dependency:\n\n```console\n$ uv add httpx\n```\n\nAn entry will be added in the `project.dependencies` field:\n\n```toml title=\"pyproject.toml\" hl_lines=\"4\"\n[project]\nname = \"example\"\nversion = \"0.1.0\"\ndependencies = [\"httpx>=0.27.2\"]\n```\n\nThe [`--dev`](#development-dependencies), [`--group`](#dependency-groups), or\n[`--optional`](#optional-dependencies) flags can be used "], "retrieved": []}
{"id": "q0032", "q": "How do you run the FastAPI app locally in development mode with uv?", "shape": "conceptual", "gold": ["guides/integration/aws-lambda.md#1"], "gold_text": ["## Getting started\n\nTo start, assume we have a minimal FastAPI application with the following structure:\n\n```plaintext\nproject\n├── pyproject.toml\n└── app\n    ├── __init__.py\n    └── main.py\n```\n\nWhere the `pyproject.toml` contains:\n\n```toml title=\"pyproject.toml\"\n[project]\nname = \"uv-aws-lambda-example\"\nversion = \"0.1.0\"\nrequires-python = \">=3.13\"\ndependencies = [\n    # FastAPI is a modern web fra"], "retrieved": ["guides/integration/fastapi.md#1"]}
{"id": "q0034", "q": "How can you show or override the directory where uv stores Python executables?", "shape": "conceptual", "gold": ["reference/storage.md#8"], "gold_text": ["### Python executables\n\nuv installs executables for [Python versions](#python-versions), e.g., `python3.13`.\n\nBy default, Python executables are stored in the [executable directory](#executable-directory).\n\nUse `uv python dir --bin` to show the Python executable directory.\n\nUse the `UV_PYTHON_BIN_DIR` environment variable to override the Python executable directory."], "retrieved": []}
{"id": "q0035", "q": "What does the `UV_PYTHON_BIN_DIR` environment variable control?", "shape": "exact_lookup", "gold": ["reference/storage.md#8"], "gold_text": ["### Python executables\n\nuv installs executables for [Python versions](#python-versions), e.g., `python3.13`.\n\nBy default, Python executables are stored in the [executable directory](#executable-directory).\n\nUse `uv python dir --bin` to show the Python executable directory.\n\nUse the `UV_PYTHON_BIN_DIR` environment variable to override the Python executable directory."], "retrieved": ["reference/storage.md#1"]}
{"id": "q0036", "q": "How do `uv sync` and `uv run` differ in whether they remove extra installed packages?", "shape": "conceptual", "gold": ["concepts/projects/sync.md#6"], "gold_text": ["### Handling of extraneous packages\n\n`uv sync` performs \"exact\" syncing by default, which means it will remove any packages that are not\npresent in the lockfile.\n\nTo retain extraneous packages, use the `--inexact` flag:\n\n```console\n$ uv sync --inexact\n```\n\nIn contrast, `uv run` uses \"inexact\" syncing by default, ensuring that all required packages are\ninstalled but not removing extraneous packages"], "retrieved": []}
{"id": "q0037", "q": "What does the `--inexact` flag do for `uv sync`?", "shape": "exact_lookup", "gold": ["concepts/projects/sync.md#6"], "gold_text": ["### Handling of extraneous packages\n\n`uv sync` performs \"exact\" syncing by default, which means it will remove any packages that are not\npresent in the lockfile.\n\nTo retain extraneous packages, use the `--inexact` flag:\n\n```console\n$ uv sync --inexact\n```\n\nIn contrast, `uv run` uses \"inexact\" syncing by default, ensuring that all required packages are\ninstalled but not removing extraneous packages"], "retrieved": []}
{"id": "q0038", "q": "When installing Python executables, when does uv overwrite an existing one?", "shape": "conceptual", "gold": ["concepts/python-versions.md#5"], "gold_text": ["### Installing Python executables\n\nuv installs Python executables into your `PATH` by default, e.g., on Unix `uv python install 3.12`\nwill install a Python executable into `~/.local/bin`, e.g., as `python3.12`. See the\n[storage documentation](../reference/storage.md#python-executables) for more details about the\ntarget directory.\n\n!!! tip\n\nIf `~/.local/bin` is not in your `PATH`, you can add it wi"], "retrieved": []}
{"id": "q0039", "q": "What does the --default option do for uv python install?", "shape": "exact_lookup", "gold": ["concepts/python-versions.md#5"], "gold_text": ["### Installing Python executables\n\nuv installs Python executables into your `PATH` by default, e.g., on Unix `uv python install 3.12`\nwill install a Python executable into `~/.local/bin`, e.g., as `python3.12`. See the\n[storage documentation](../reference/storage.md#python-executables) for more details about the\ntarget directory.\n\n!!! tip\n\nIf `~/.local/bin` is not in your `PATH`, you can add it wi"], "retrieved": []}
{"id": "q0040", "q": "How do you run Jupyter in a virtual environment that has no project files like pyproject.toml or uv.lock?", "shape": "conceptual", "gold": ["guides/integration/jupyter.md#5"], "gold_text": ["## Using Jupyter with a non-project environment\n\nIf you need to run Jupyter in a virtual environment that isn't associated with a\n[project](../../concepts/projects/index.md) (e.g., has no `pyproject.toml` or `uv.lock`), you can do\nso by adding Jupyter to the environment directly. For example:\n\n=== \"macOS and Linux\"\n\n    ```console\n    $ uv venv --seed\n    $ uv pip install pydantic\n    $ uv pip ins"], "retrieved": ["guides/integration/jupyter.md#1"]}
{"id": "q0041", "q": "How can I sync development dependencies without installing the project itself?", "shape": "conceptual", "gold": ["concepts/projects/sync.md#8"], "gold_text": ["### Syncing development dependencies\n\nuv reads development dependencies from the `[dependency-groups]` table (as defined in\n[PEP 735](https://peps.python.org/pep-0735/)).\n\nThe `dev` group is special-cased and synced by default. See the\n[default groups](./dependencies.md#default-groups) documentation for details on changing the\ndefaults.\n\nThe `--no-dev` flag can be used to exclude the `dev` group.\n"], "retrieved": ["concepts/projects/sync.md#2"]}
{"id": "q0042", "q": "What does the `--only-group` option do when syncing dependencies?", "shape": "exact_lookup", "gold": ["concepts/projects/sync.md#8"], "gold_text": ["### Syncing development dependencies\n\nuv reads development dependencies from the `[dependency-groups]` table (as defined in\n[PEP 735](https://peps.python.org/pep-0735/)).\n\nThe `dev` group is special-cased and synced by default. See the\n[default groups](./dependencies.md#default-groups) documentation for details on changing the\ndefaults.\n\nThe `--no-dev` flag can be used to exclude the `dev` group.\n"], "retrieved": []}
{"id": "q0043", "q": "How does uv choose where to create a new project, and what happens if that directory already has a project file?", "shape": "conceptual", "gold": ["concepts/projects/init.md#1"], "gold_text": ["## Target directory\n\nuv will create a project in the working directory, or, in a target directory by providing a name,\ne.g., `uv init foo`. The working directory can be modified with the `--directory` option, which will\ncause the target directory path to be interpreted relative to the specified working directory. If\nthere's already a project in the target directory, i.e., if there's a `pyproject.t"], "retrieved": []}
{"id": "q0044", "q": "What does the `--directory` option do when running `uv init`?", "shape": "exact_lookup", "gold": ["concepts/projects/init.md#1"], "gold_text": ["## Target directory\n\nuv will create a project in the working directory, or, in a target directory by providing a name,\ne.g., `uv init foo`. The working directory can be modified with the `--directory` option, which will\ncause the target directory path to be interpreted relative to the specified working directory. If\nthere's already a project in the target directory, i.e., if there's a `pyproject.t"], "retrieved": []}
{"id": "q0045", "q": "How can uv be configured to use the subprocess keyring provider for fetching credentials?", "shape": "conceptual", "gold": ["concepts/authentication/http.md#3"], "gold_text": ["## Keyring providers\n\nA keyring provider is a concept from `pip` allowing retrieval of credentials from an interface\nmatching the popular [keyring](https://github.com/jaraco/keyring) Python package.\n\nThe \"subprocess\" keyring provider invokes the `keyring` command to fetch credentials. uv does not\nsupport additional keyring provider types at this time.\n\nSet `--keyring-provider subprocess`, `UV_KEYR"], "retrieved": []}
{"id": "q0046", "q": "What does `UV_KEYRING_PROVIDER` control?", "shape": "exact_lookup", "gold": ["concepts/authentication/http.md#3"], "gold_text": ["## Keyring providers\n\nA keyring provider is a concept from `pip` allowing retrieval of credentials from an interface\nmatching the popular [keyring](https://github.com/jaraco/keyring) Python package.\n\nThe \"subprocess\" keyring provider invokes the `keyring` command to fetch credentials. uv does not\nsupport additional keyring provider types at this time.\n\nSet `--keyring-provider subprocess`, `UV_KEYR"], "retrieved": []}
{"id": "q0047", "q": "What happens if a locked dependency matches a malware advisory during sync?", "shape": "conceptual", "gold": ["concepts/projects/sync.md#12"], "gold_text": ["## Malware checks\n\n!!! important\n\nOn-sync malware checking is in [preview](../preview.md), and is subject to change until stabilized.\n\nWhile syncing, uv can perform a lightweight scan of your lockfile for known malware by checking it\nagainst [OSV](https://osv.dev). OSV references MAL advisories from the OpenSSF's\n[malicious packages database](https://github.com/ossf/malicious-packages).\n\nIf a lock"], "retrieved": []}
{"id": "q0048", "q": "What does the UV_MALWARE_CHECK_URL environment variable control?", "shape": "exact_lookup", "gold": ["concepts/projects/sync.md#12"], "gold_text": ["## Malware checks\n\n!!! important\n\nOn-sync malware checking is in [preview](../preview.md), and is subject to change until stabilized.\n\nWhile syncing, uv can perform a lightweight scan of your lockfile for known malware by checking it\nagainst [OSV](https://osv.dev). OSV references MAL advisories from the OpenSSF's\n[malicious packages database](https://github.com/ossf/malicious-packages).\n\nIf a lock"], "retrieved": []}
{"id": "q0049", "q": "How do you enable a post-job hook on a self-hosted GitHub runner for cleaning the uv cache?", "shape": "conceptual", "gold": ["guides/integration/github.md#6"], "gold_text": ["Using a post job hook requires setting the `ACTIONS_RUNNER_HOOK_JOB_STARTED` environment\n    variable on the self-hosted runner to the path of a cleanup script such as the one shown below.\n\n    ```sh title=\"clean-uv-cache.sh\"\n    #!/usr/bin/env sh\n    uv cache clean\n    ```"], "retrieved": []}
{"id": "q0050", "q": "What does the ACTIONS_RUNNER_HOOK_JOB_STARTED environment variable specify on a self-hosted runner?", "shape": "exact_lookup", "gold": ["guides/integration/github.md#6"], "gold_text": ["Using a post job hook requires setting the `ACTIONS_RUNNER_HOOK_JOB_STARTED` environment\n    variable on the self-hosted runner to the path of a cleanup script such as the one shown below.\n\n    ```sh title=\"clean-uv-cache.sh\"\n    #!/usr/bin/env sh\n    uv cache clean\n    ```"], "retrieved": ["guides/integration/github.md#1"]}
{"id": "q0051", "q": "How do you run a local `uv run` command as a Coiled batch job in the cloud?", "shape": "conceptual", "gold": ["guides/integration/coiled.md#3"], "gold_text": ["To submit a batch job for Coiled to run, use\n[`coiled batch run`](https://docs.coiled.io/user_guide/api.html?utm_source=uv-docs#coiled-batch-run)\nto execute the `uv run` command in the cloud:\n\n```bash hl_lines=\"1\"\n$ uvx coiled batch run \\\n    uv run process.py\n```\n\nThe same process that previously ran locally is now running on a remote cloud VM on AWS.\n\nYou can monitor the progress of the batch jo"], "retrieved": ["guides/integration/coiled.md#1"]}
{"id": "q0052", "q": "What does `coiled batch run` do?", "shape": "exact_lookup", "gold": ["guides/integration/coiled.md#3"], "gold_text": ["To submit a batch job for Coiled to run, use\n[`coiled batch run`](https://docs.coiled.io/user_guide/api.html?utm_source=uv-docs#coiled-batch-run)\nto execute the `uv run` command in the cloud:\n\n```bash hl_lines=\"1\"\n$ uvx coiled batch run \\\n    uv run process.py\n```\n\nThe same process that previously ran locally is now running on a remote cloud VM on AWS.\n\nYou can monitor the progress of the batch jo"], "retrieved": []}
{"id": "q0053", "q": "How do you change a package version to a specific release without editing pyproject.toml directly?", "shape": "conceptual", "gold": ["guides/package.md#3"], "gold_text": ["## Updating your version\n\nThe `uv version` command provides conveniences for updating the version of your package before you\npublish it.\n[See the project docs for reading your package's version](./projects.md#viewing-your-version).\n\nTo update to an exact version, provide it as a positional argument:\n\n```console\n$ uv version 1.0.0\nhello-world 0.7.0 => 1.0.0\n```\n\nTo preview the change without updati"], "retrieved": []}
{"id": "q0054", "q": "What does the `--dry-run` flag do for `uv version`?", "shape": "exact_lookup", "gold": ["guides/package.md#3"], "gold_text": ["## Updating your version\n\nThe `uv version` command provides conveniences for updating the version of your package before you\npublish it.\n[See the project docs for reading your package's version](./projects.md#viewing-your-version).\n\nTo update to an exact version, provide it as a positional argument:\n\n```console\n$ uv version 1.0.0\nhello-world 0.7.0 => 1.0.0\n```\n\nTo preview the change without updati"], "retrieved": []}
{"id": "q0055", "q": "How do you upgrade an installed Python version to the latest supported patch release with uv?", "shape": "conceptual", "gold": ["guides/install-python.md#7"], "gold_text": ["## Upgrading Python versions\n\n!!! important\n\nSupport for upgrading Python patch versions is in _preview_. This means the behavior is\n    experimental and subject to change.\n\nTo upgrade a Python version to the latest supported patch release:\n\n```console\n$ uv python upgrade 3.12\n```\n\nTo upgrade all uv-managed Python versions:\n\n```console\n$ uv python upgrade\n```\n\nSee the [`python upgrade`](../concept"], "retrieved": []}
{"id": "q0056", "q": "What does `uv python upgrade` do?", "shape": "exact_lookup", "gold": ["guides/install-python.md#7"], "gold_text": ["## Upgrading Python versions\n\n!!! important\n\nSupport for upgrading Python patch versions is in _preview_. This means the behavior is\n    experimental and subject to change.\n\nTo upgrade a Python version to the latest supported patch release:\n\n```console\n$ uv python upgrade 3.12\n```\n\nTo upgrade all uv-managed Python versions:\n\n```console\n$ uv python upgrade\n```\n\nSee the [`python upgrade`](../concept"], "retrieved": ["guides/install-python.md#1"]}
{"id": "q0057", "q": "How can uv export a lockfile for use with other tools and workflows?", "shape": "conceptual", "gold": ["concepts/projects/export.md#0"], "gold_text": ["# Exporting a lockfile\n\nuv can export a lockfile to different formats for integration with other tools and workflows. The\n`uv export` command supports multiple output formats, each suited to different use cases.\n\nFor more details on lockfiles and how they're created, see the [project layout](./layout.md) and\n[locking and syncing](./sync.md) documentation."], "retrieved": ["concepts/projects/export.md#1"]}
{"id": "q0058", "q": "What does the `uv export` command do?", "shape": "exact_lookup", "gold": ["concepts/projects/export.md#0"], "gold_text": ["# Exporting a lockfile\n\nuv can export a lockfile to different formats for integration with other tools and workflows. The\n`uv export` command supports multiple output formats, each suited to different use cases.\n\nFor more details on lockfiles and how they're created, see the [project layout](./layout.md) and\n[locking and syncing](./sync.md) documentation."], "retrieved": ["concepts/projects/export.md#1"]}
{"id": "q0059", "q": "When can uv reject an existing lockfile because of its schema version?", "shape": "conceptual", "gold": ["concepts/resolution.md#30"], "gold_text": ["## Lockfile versioning\n\nThe `uv.lock` file uses a versioned schema. The schema version is included in the `version` field of\nthe lockfile.\n\nAny given version of uv can read and write lockfiles with the same schema version, but will reject\nlockfiles with a greater schema version. For example, if your uv version supports schema v1,\n`uv lock` will error if it encounters an existing lockfile with sche"], "retrieved": []}
{"id": "q0060", "q": "What does the `revision` field in `uv.lock` track?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#30"], "gold_text": ["## Lockfile versioning\n\nThe `uv.lock` file uses a versioned schema. The schema version is included in the `version` field of\nthe lockfile.\n\nAny given version of uv can read and write lockfiles with the same schema version, but will reject\nlockfiles with a greater schema version. For example, if your uv version supports schema v1,\n`uv lock` will error if it encounters an existing lockfile with sche"], "retrieved": []}
{"id": "q0061", "q": "How do I list only installed Python versions instead of downloads too?", "shape": "conceptual", "gold": ["concepts/python-versions.md#9"], "gold_text": ["## Viewing available Python versions\n\nTo list installed and available Python versions:\n\n```console\n$ uv python list\n```\n\nTo filter the Python versions, provide a request, e.g., to show all Python 3.13 interpreters:\n\n```console\n$ uv python list 3.13\n```\n\nOr, to show all PyPy interpreters:\n\n```console\n$ uv python list pypy\n```\n\nBy default, downloads for other platforms and old patch versions are hid"], "retrieved": []}
{"id": "q0062", "q": "What does --all-versions do in uv python list?", "shape": "exact_lookup", "gold": ["concepts/python-versions.md#9"], "gold_text": ["## Viewing available Python versions\n\nTo list installed and available Python versions:\n\n```console\n$ uv python list\n```\n\nTo filter the Python versions, provide a request, e.g., to show all Python 3.13 interpreters:\n\n```console\n$ uv python list 3.13\n```\n\nOr, to show all PyPy interpreters:\n\n```console\n$ uv python list pypy\n```\n\nBy default, downloads for other platforms and old patch versions are hid"], "retrieved": []}
{"id": "q0063", "q": "How should Docker image attestations be verified for a released uv image instead of using the latest tag?", "shape": "conceptual", "gold": ["guides/integration/docker.md#20"], "gold_text": ["```console\n$ REPO=astral-sh/uv\n$ gh attestation download --repo $REPO oci://ghcr.io/${REPO}:latest\nWrote attestations to file sha256:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.jsonl.\nAny previous content has been overwritten\n\nThe trusted metadata is now available at sha256:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.jsonl\n$ docker buildx imagetools inspec"], "retrieved": ["guides/integration/docker.md#1"]}
{"id": "q0064", "q": "Why should library tests be run with lowest dependency resolution in continuous integration?", "shape": "conceptual", "gold": ["concepts/resolution.md#13"], "gold_text": ["When publishing libraries, it is recommended to separately run tests with `--resolution lowest` or\n`--resolution lowest-direct` in continuous integration to ensure compatibility with the declared\nlower bounds."], "retrieved": []}
{"id": "q0065", "q": "How are dependency version ranges combined in uv requirement strings?", "shape": "conceptual", "gold": ["concepts/projects/dependencies.md#29"], "gold_text": ["## Dependency specifiers\n\nuv uses standard\n[dependency specifiers](https://packaging.python.org/en/latest/specifications/dependency-specifiers/),\noriginally defined in [PEP 508](https://peps.python.org/pep-0508/). A dependency specifier is\ncomposed of, in order:\n\n- The dependency name\n- The extras you want (optional)\n- The version specifier\n- An environment marker (optional)\n\nThe version specifier"], "retrieved": ["concepts/resolution.md#1"]}
{"id": "q0066", "q": "How do you update uv after installing it with the standalone installer?", "shape": "conceptual", "gold": ["getting-started/installation.md#10"], "gold_text": ["## Upgrading uv\n\nWhen uv is installed via the standalone installer, it can update itself on-demand:\n\n```console\n$ uv self update\n```\n\n!!! tip\n\nUpdating uv will re-run the installer and can modify your shell profiles. To disable this\n    behavior, set `UV_NO_MODIFY_PATH=1`.\n\nWhen another installation method is used, self-updates are disabled. Use the package manager's\nupgrade method instead. For ex"], "retrieved": []}
{"id": "q0067", "q": "What does the `UV_NO_MODIFY_PATH` setting do when updating uv?", "shape": "exact_lookup", "gold": ["getting-started/installation.md#10"], "gold_text": ["## Upgrading uv\n\nWhen uv is installed via the standalone installer, it can update itself on-demand:\n\n```console\n$ uv self update\n```\n\n!!! tip\n\nUpdating uv will re-run the installer and can modify your shell profiles. To disable this\n    behavior, set `UV_NO_MODIFY_PATH=1`.\n\nWhen another installation method is used, self-updates are disabled. Use the package manager's\nupgrade method instead. For ex"], "retrieved": ["reference/installer.md#1"]}
{"id": "q0068", "q": "What is the difference between direct dependencies and transitive dependencies in a Python package?", "shape": "conceptual", "gold": ["concepts/resolution.md#1"], "gold_text": ["## Dependencies\n\nMost projects and packages have dependencies. Dependencies are other packages that are necessary in\norder for the current package to work. A package defines its dependencies as _requirements_, roughly\na combination of a package name and acceptable versions. The dependencies defined by the current\nproject are called _direct dependencies_. The dependencies added by each dependency o"], "retrieved": ["concepts/projects/dependencies.md#1"]}
{"id": "q0069", "q": "How do editable packages behave when their source code changes?", "shape": "conceptual", "gold": ["pip/packages.md#1"], "gold_text": ["## Editable packages\n\nEditable packages do not need to be reinstalled for changes to their source code to be active.\n\nTo install the current project as an editable package\n\n```console\n$ uv pip install -e .\n```\n\nTo install a project in another directory as an editable package:\n\n```console\n$ uv pip install -e \"ruff @ ./project/ruff\"\n```"], "retrieved": []}
{"id": "q0070", "q": "What are entry points used for in an installed Python package?", "shape": "conceptual", "gold": ["concepts/projects/config.md#1"], "gold_text": ["## Entry points\n\n[Entry points](https://packaging.python.org/en/latest/specifications/entry-points/#entry-points) are\nthe official term for an installed package to advertise interfaces. These include:\n\n- [Command line interfaces](#command-line-interfaces)\n- [Graphical user interfaces](#graphical-user-interfaces)\n- [Plugin entry points](#plugin-entry-points)\n\n!!! important\n\nUsing the entry point ta"], "retrieved": ["concepts/projects/config.md#entry-points"]}
{"id": "q0071", "q": "What happens when a project has no build-system definition in pyproject.toml during uv sync and uv build?", "shape": "conceptual", "gold": ["guides/package.md#1"], "gold_text": ["## Preparing your project\n\nBefore attempting to publish your project, you'll want to make sure it's ready to be packaged for\ndistribution.\n\nIf your project does not include a `[build-system]` definition in the `pyproject.toml`, uv will not\nbuild it during `uv sync` operations in the project, but will fall back to the legacy setuptools\nbuild system during `uv build`.\n\n!!! note\n\nProjects created wit"], "retrieved": ["concepts/projects/build.md#1"]}
{"id": "q0072", "q": "What does a [build-system] definition in pyproject.toml control?", "shape": "exact_lookup", "gold": ["guides/package.md#1"], "gold_text": ["## Preparing your project\n\nBefore attempting to publish your project, you'll want to make sure it's ready to be packaged for\ndistribution.\n\nIf your project does not include a `[build-system]` definition in the `pyproject.toml`, uv will not\nbuild it during `uv sync` operations in the project, but will fall back to the legacy setuptools\nbuild system during `uv build`.\n\n!!! note\n\nProjects created wit"], "retrieved": []}
{"id": "q0073", "q": "Why would uv fail to resolve a project with incompatible optional dependency extras?", "shape": "conceptual", "gold": ["concepts/resolution.md#22"], "gold_text": ["## Conflicting dependencies\n\nuv requires that all dependencies declared by a project are compatible with each other and resolves\nall dependencies together when creating the lockfile. This includes project dependencies, optional\ndependencies (\"extras\"), and dependency groups (development dependencies).\n\nIf dependencies declared in one extra are not compatible with those in another extra, uv will fa"], "retrieved": []}
{"id": "q0074", "q": "What does the `conflicts` setting in `tool.uv` do?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#22"], "gold_text": ["## Conflicting dependencies\n\nuv requires that all dependencies declared by a project are compatible with each other and resolves\nall dependencies together when creating the lockfile. This includes project dependencies, optional\ndependencies (\"extras\"), and dependency groups (development dependencies).\n\nIf dependencies declared in one extra are not compatible with those in another extra, uv will fa"], "retrieved": []}
{"id": "q0075", "q": "How can you change where uv installs its executables?", "shape": "conceptual", "gold": ["reference/storage.md#11"], "gold_text": ["### The uv executable\n\nWhen using uv's [standalone installer](./installer.md) to install uv, the `uv` and `uvx` executables\nare installed into the [executable directory](#executable-directory).\n\nUse the `UV_INSTALL_DIR` environment variable to configure uv's installation directory."], "retrieved": []}
{"id": "q0076", "q": "What does the UV_INSTALL_DIR environment variable configure?", "shape": "exact_lookup", "gold": ["reference/storage.md#11"], "gold_text": ["### The uv executable\n\nWhen using uv's [standalone installer](./installer.md) to install uv, the `uv` and `uvx` executables\nare installed into the [executable directory](#executable-directory).\n\nUse the `UV_INSTALL_DIR` environment variable to configure uv's installation directory."], "retrieved": ["reference/installer.md#1"]}
{"id": "q0077", "q": "Why would you sync with a build optional group first, then sync again without it?", "shape": "conceptual", "gold": ["concepts/projects/config.md#16"], "gold_text": ["```toml title=\"pyproject.toml\"\n[project]\nname = \"project\"\nversion = \"0.1.0\"\ndescription = \"...\"\nreadme = \"README.md\"\nrequires-python = \">=3.12\"\ndependencies = [\"flash-attn\", \"torch\"]\n\n[tool.uv]\nno-build-isolation-package = [\"flash-attn\"]\n```\n\nWhen running `uv sync`, uv will first install `torch` in the project environment, followed by\n`flash-attn` (without build isolation). As `torch` is both a pr"], "retrieved": []}
{"id": "q0078", "q": "What does no-build-isolation-package control in uv?", "shape": "exact_lookup", "gold": ["concepts/projects/config.md#16"], "gold_text": ["```toml title=\"pyproject.toml\"\n[project]\nname = \"project\"\nversion = \"0.1.0\"\ndescription = \"...\"\nreadme = \"README.md\"\nrequires-python = \">=3.12\"\ndependencies = [\"flash-attn\", \"torch\"]\n\n[tool.uv]\nno-build-isolation-package = [\"flash-attn\"]\n```\n\nWhen running `uv sync`, uv will first install `torch` in the project environment, followed by\n`flash-attn` (without build isolation). As `torch` is both a pr"], "retrieved": []}
{"id": "q0079", "q": "How can I publish a package to AWS CodeArtifact with uv?", "shape": "conceptual", "gold": ["guides/integration/aws.md#3"], "gold_text": ["## Publishing packages\n\nIf you also want to publish your own packages to AWS CodeArtifact, you can use `uv publish` as\ndescribed in the [Building and publishing guide](../package.md).\n\nFirst, add a `publish-url` to the index you want to publish packages to. For example:\n\n```toml title=\"pyproject.toml\" hl_lines=\"4\"\n[[tool.uv.index]]\nname = \"private-registry\"\nurl = \"https://<DOMAIN>-<ACCOUNT_ID>.d.c"], "retrieved": ["guides/integration/aws.md#1"]}
{"id": "q0080", "q": "What does UV_PUBLISH_URL do when publishing to AWS CodeArtifact?", "shape": "exact_lookup", "gold": ["guides/integration/aws.md#3"], "gold_text": ["## Publishing packages\n\nIf you also want to publish your own packages to AWS CodeArtifact, you can use `uv publish` as\ndescribed in the [Building and publishing guide](../package.md).\n\nFirst, add a `publish-url` to the index you want to publish packages to. For example:\n\n```toml title=\"pyproject.toml\" hl_lines=\"4\"\n[[tool.uv.index]]\nname = \"private-registry\"\nurl = \"https://<DOMAIN>-<ACCOUNT_ID>.d.c"], "retrieved": ["guides/integration/aws.md#1"]}
{"id": "q0082", "q": "What does Tier 2 support mean for Python implementations in uv?", "shape": "exact_lookup", "gold": ["reference/policies/python.md#1"], "gold_text": ["## Python implementations\n\nuv has Tier 1 support for the following Python implementations:\n\n- CPython\n\nAs with [platforms](./platforms.md), Tier 1 support can be thought of \"guaranteed to work\". uv\nsupports managed installations of these implementations, and the builds are maintained by Astral.\n\nuv has Tier 2 support for:\n\n- PyPy\n- GraalPy\n- Pyodide\n\nuv is \"expected to work\" with these implementat"], "retrieved": ["reference/policies/python.md#2"]}
{"id": "q0083", "q": "How can you check that uv is installed and available?", "shape": "conceptual", "gold": ["getting-started/first-steps.md#0"], "gold_text": ["# First steps with uv\n\nAfter [installing uv](./installation.md), you can check that uv is available by running the `uv`\ncommand:\n\n```console\n$ uv\nAn extremely fast Python package manager.\n\nUsage: uv [OPTIONS] <COMMAND>\n\n...\n```\n\nYou should see a help menu listing the available commands."], "retrieved": []}
{"id": "q0084", "q": "What file format does uv use for configuration files, and where does it look for them?", "shape": "conceptual", "gold": ["reference/storage.md#12"], "gold_text": ["### Configuration files\n\nuv's behavior can be configured through TOML files.\n\nConfiguration files are discovered in the [configuration directories](#configuration-directories).\n\nFor more details, see the [configuration files documentation](../concepts/configuration-files.md)."], "retrieved": []}
{"id": "q0085", "q": "When can a path or workspace dependency be treated as virtual instead of installed as a package?", "shape": "conceptual", "gold": ["concepts/projects/dependencies.md#27"], "gold_text": ["## Virtual dependencies\n\nuv allows dependencies to be \"virtual\", in which the dependency itself is not installed as a\n[package](./config.md#project-packaging), but its dependencies are.\n\nBy default, dependencies are never virtual.\n\nA dependency with a [`path` source](#path) can be virtual if it explicitly sets\n[`tool.uv.package = false`](../../reference/settings.md#package). Without this setting, "], "retrieved": []}
{"id": "q0086", "q": "What does `tool.uv.package = false` do for a dependency source?", "shape": "exact_lookup", "gold": ["concepts/projects/dependencies.md#27"], "gold_text": ["## Virtual dependencies\n\nuv allows dependencies to be \"virtual\", in which the dependency itself is not installed as a\n[package](./config.md#project-packaging), but its dependencies are.\n\nBy default, dependencies are never virtual.\n\nA dependency with a [`path` source](#path) can be virtual if it explicitly sets\n[`tool.uv.package = false`](../../reference/settings.md#package). Without this setting, "], "retrieved": ["concepts/projects/dependencies.md#1"]}
{"id": "q0087", "q": "How does uv choose package versions differently under the default fork strategy versus `fewest`?", "shape": "conceptual", "gold": ["concepts/resolution.md#15"], "gold_text": ["## Multi-version resolution\n\nDuring universal resolution, a package may be listed multiple times with different versions or URLs\nwithin the same lockfile, since different versions may be needed for different platforms or Python\nversions.\n\nThe `--fork-strategy` setting can be used to control how uv trades off between (1) minimizing the\nnumber of selected versions and (2) selecting the latest-possib"], "retrieved": []}
{"id": "q0088", "q": "What does `--fork-strategy fewest` do?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#15"], "gold_text": ["## Multi-version resolution\n\nDuring universal resolution, a package may be listed multiple times with different versions or URLs\nwithin the same lockfile, since different versions may be needed for different platforms or Python\nversions.\n\nThe `--fork-strategy` setting can be used to control how uv trades off between (1) minimizing the\nnumber of selected versions and (2) selecting the latest-possib"], "retrieved": []}
{"id": "q0089", "q": "How do you create a Python project without setting up a build system in uv?", "shape": "conceptual", "gold": ["concepts/projects/init.md#6"], "gold_text": ["## Creating a project without a build system\n\nWhile defining a build system generally provides a better experience, there are some cases in which\nit can be easier to omit it and define your Python modules directly in the top-level directory.\n\nUse the `--no-package` flag to disable using a build system:\n\n```console\n$ uv init --no-package example-app\n```\n\nThe project includes a `pyproject.toml`, a s"], "retrieved": ["concepts/projects/init.md#2"]}
{"id": "q0090", "q": "What does the `--no-package` flag do in `uv init`?", "shape": "exact_lookup", "gold": ["concepts/projects/init.md#6"], "gold_text": ["## Creating a project without a build system\n\nWhile defining a build system generally provides a better experience, there are some cases in which\nit can be easier to omit it and define your Python modules directly in the top-level directory.\n\nUse the `--no-package` flag to disable using a build system:\n\n```console\n$ uv init --no-package example-app\n```\n\nThe project includes a `pyproject.toml`, a s"], "retrieved": ["concepts/projects/init.md#1"]}
{"id": "q0091", "q": "What happens when uv sync is run with --no-install-project?", "shape": "conceptual", "gold": ["concepts/projects/sync.md#11"], "gold_text": ["## Partial installations\n\nSometimes it's helpful to perform installations in multiple steps, e.g., for optimal layer caching\nwhile building a Docker image. `uv sync` has several flags for this purpose.\n\n- `--no-install-project`: Do not install the current project\n- `--no-install-workspace`: Do not install any workspace members, including the root project\n- `--no-install-package <NO_INSTALL_PACKAGE"], "retrieved": []}
{"id": "q0092", "q": "What does the --no-install-workspace flag do in uv sync?", "shape": "exact_lookup", "gold": ["concepts/projects/sync.md#11"], "gold_text": ["## Partial installations\n\nSometimes it's helpful to perform installations in multiple steps, e.g., for optimal layer caching\nwhile building a Docker image. `uv sync` has several flags for this purpose.\n\n- `--no-install-project`: Do not install the current project\n- `--no-install-workspace`: Do not install any workspace members, including the root project\n- `--no-install-package <NO_INSTALL_PACKAGE"], "retrieved": []}
{"id": "q0093", "q": "What does uv init do when you use the --bare option?", "shape": "conceptual", "gold": ["concepts/projects/init.md#7"], "gold_text": ["## Creating a minimal project\n\nIf you only want to create a `pyproject.toml`, use the `--bare` option:\n\n```console\n$ uv init example-bare --bare\n```\n\nuv will skip creating a Python version pin file, a README, and any source directories or files.\nAdditionally, uv will not initialize a version control system (i.e., `git`).\n\n```console\n$ tree example-bare\nexample-bare\n└── pyproject.toml\n```\n\nuv will "], "retrieved": ["concepts/projects/init.md#1"]}
{"id": "q0094", "q": "What does the --bare option do in uv init?", "shape": "exact_lookup", "gold": ["concepts/projects/init.md#7"], "gold_text": ["## Creating a minimal project\n\nIf you only want to create a `pyproject.toml`, use the `--bare` option:\n\n```console\n$ uv init example-bare --bare\n```\n\nuv will skip creating a Python version pin file, a README, and any source directories or files.\nAdditionally, uv will not initialize a version control system (i.e., `git`).\n\n```console\n$ tree example-bare\nexample-bare\n└── pyproject.toml\n```\n\nuv will "], "retrieved": []}
{"id": "q0095", "q": "How does uv choose between free-threaded and GIL-enabled Python versions on Python 3.14+?", "shape": "conceptual", "gold": ["concepts/python-versions.md#13"], "gold_text": ["## Free-threaded Python\n\nuv supports discovering and installing\n[free-threaded](https://docs.python.org/3.14/glossary.html#term-free-threading) Python variants in\nCPython 3.13+.\n\nFor Python 3.13, free-threaded Python versions will not be selected by default. Free-threaded Python\nversions will only be selected when explicitly requested, e.g., with `3.13t` or `3.13+freethreaded`.\n\nFor Python 3.14+, "], "retrieved": []}
{"id": "q0096", "q": "What does the +gil variant specifier require?", "shape": "exact_lookup", "gold": ["concepts/python-versions.md#13"], "gold_text": ["## Free-threaded Python\n\nuv supports discovering and installing\n[free-threaded](https://docs.python.org/3.14/glossary.html#term-free-threading) Python variants in\nCPython 3.13+.\n\nFor Python 3.13, free-threaded Python versions will not be selected by default. Free-threaded Python\nversions will only be selected when explicitly requested, e.g., with `3.13t` or `3.13+freethreaded`.\n\nFor Python 3.14+, "], "retrieved": []}
{"id": "q0097", "q": "How do you run a script that needs extra packages without adding them to a long-lived virtual environment?", "shape": "conceptual", "gold": ["guides/scripts.md#2"], "gold_text": ["## Running a script with dependencies\n\nWhen your script requires other packages, they must be installed into the environment that the\nscript runs in. uv prefers to create these environments on-demand instead of using a long-lived\nvirtual environment with manually managed dependencies. This requires explicit declaration of\ndependencies that are required for the script. Generally, it's recommended t"], "retrieved": []}
{"id": "q0098", "q": "What does the `--with` option do in `uv run`?", "shape": "exact_lookup", "gold": ["guides/scripts.md#2"], "gold_text": ["## Running a script with dependencies\n\nWhen your script requires other packages, they must be installed into the environment that the\nscript runs in. uv prefers to create these environments on-demand instead of using a long-lived\nvirtual environment with manually managed dependencies. This requires explicit declaration of\ndependencies that are required for the script. Generally, it's recommended t"], "retrieved": []}
{"id": "q0099", "q": "When building a source distribution, do excluded files override included files, or vice versa?", "shape": "conceptual", "gold": ["concepts/build-backend.md#7"], "gold_text": ["## File inclusion and exclusion\n\nThe build backend is responsible for determining which files in a source tree should be packaged\ninto the distributions.\n\nTo determine which files to include in a source distribution, uv first adds the included files and\ndirectories, then removes the excluded files and directories. This means that exclusions always take\nprecedence over inclusions.\n\nBy default, uv e"], "retrieved": []}
{"id": "q0100", "q": "What does `toml-backwards-compatibility` do when uv detects TOML 1.1-only syntax?", "shape": "exact_lookup", "gold": ["concepts/build-backend.md#7"], "gold_text": ["## File inclusion and exclusion\n\nThe build backend is responsible for determining which files in a source tree should be packaged\ninto the distributions.\n\nTo determine which files to include in a source distribution, uv first adds the included files and\ndirectories, then removes the excluded files and directories. This means that exclusions always take\nprecedence over inclusions.\n\nBy default, uv e"], "retrieved": []}
{"id": "q0101", "q": "How can workspace members be declared incompatible with each other in a pyproject configuration?", "shape": "conceptual", "gold": ["concepts/resolution.md#24"], "gold_text": ["```toml title=\"member1/pyproject.toml\"\n[project]\nname = \"member1\"\ndependencies = [\"numpy==2.1.2\"]\n```\n\n```toml title=\"member2/pyproject.toml\"\n[project]\nname = \"member2\"\ndependencies = [\"numpy==2.0.0\"]\n```\n\nThis conflict can also be declared using the `package` key:\n\n```toml title=\"pyproject.toml\"\n[tool.uv]\nconflicts = [\n    [\n      { package = \"member1\" },\n      { package = \"member2\" },\n    ],\n]\n`"], "retrieved": []}
{"id": "q0102", "q": "How does uv decide whether to use an existing system Python installation?", "shape": "conceptual", "gold": ["guides/install-python.md#6"], "gold_text": ["## Using existing Python versions\n\nuv will use existing Python installations if present on your system. There is no configuration\nnecessary for this behavior: uv will use the system Python if it satisfies the requirements of the\ncommand invocation. See the\n[Python discovery](../concepts/python-versions.md#discovery-of-python-versions) documentation for\ndetails.\n\nTo force uv to use the system Pytho"], "retrieved": []}
{"id": "q0103", "q": "What does the `--no-managed-python` flag do in uv?", "shape": "exact_lookup", "gold": ["guides/install-python.md#6"], "gold_text": ["## Using existing Python versions\n\nuv will use existing Python installations if present on your system. There is no configuration\nnecessary for this behavior: uv will use the system Python if it satisfies the requirements of the\ncommand invocation. See the\n[Python discovery](../concepts/python-versions.md#discovery-of-python-versions) documentation for\ndetails.\n\nTo force uv to use the system Pytho"], "retrieved": []}
{"id": "q0104", "q": "How can bytecode compilation be enabled during uv installs?", "shape": "conceptual", "gold": ["pip/compatibility.md#15"], "gold_text": ["## Bytecode compilation\n\nUnlike `pip`, uv does not compile `.py` files to `.pyc` files during installation by default (i.e.,\nuv does not create or populate `__pycache__` directories). To enable bytecode compilation during\ninstalls, pass the `--compile-bytecode` flag to `uv pip install` or `uv pip sync`, or set the\n`UV_COMPILE_BYTECODE` environment variable to `1`.\n\nSkipping bytecode compilation ca"], "retrieved": []}
{"id": "q0105", "q": "What does the UV_COMPILE_BYTECODE environment variable control?", "shape": "exact_lookup", "gold": ["pip/compatibility.md#15"], "gold_text": ["## Bytecode compilation\n\nUnlike `pip`, uv does not compile `.py` files to `.pyc` files during installation by default (i.e.,\nuv does not create or populate `__pycache__` directories). To enable bytecode compilation during\ninstalls, pass the `--compile-bytecode` flag to `uv pip install` or `uv pip sync`, or set the\n`UV_COMPILE_BYTECODE` environment variable to `1`.\n\nSkipping bytecode compilation ca"], "retrieved": []}
{"id": "q0106", "q": "How does pyproject.toml replace requirements.in files in a uv project?", "shape": "conceptual", "gold": ["guides/migration/pip-to-project.md#7"], "gold_text": ["## Migrating to a uv project\n\n### The `pyproject.toml`\n\nThe `pyproject.toml` is a standardized file for Python project metadata. It replaces\n`requirements.in` files, allowing you to represent arbitrary groups of project dependencies. It also\nprovides a centralized location for metadata about your project, such as the build system or tool\nsettings.\n\n<!-- TODO: Link to the official docs on this or w"], "retrieved": ["guides/migration/pip-to-project.md#1"]}
{"id": "q0107", "q": "What does the [dependency-groups] dev entry define in pyproject.toml?", "shape": "exact_lookup", "gold": ["guides/migration/pip-to-project.md#7"], "gold_text": ["## Migrating to a uv project\n\n### The `pyproject.toml`\n\nThe `pyproject.toml` is a standardized file for Python project metadata. It replaces\n`requirements.in` files, allowing you to represent arbitrary groups of project dependencies. It also\nprovides a centralized location for metadata about your project, such as the build system or tool\nsettings.\n\n<!-- TODO: Link to the official docs on this or w"], "retrieved": []}
{"id": "q0108", "q": "How do you upgrade just one dependency without changing the rest of the lockfile?", "shape": "conceptual", "gold": ["guides/projects.md#7"], "gold_text": ["## Managing dependencies\n\nYou can add dependencies to your `pyproject.toml` with the `uv add` command. This will also update\nthe lockfile and project environment:\n\n```console\n$ uv add requests\n```\n\nYou can also specify version constraints or alternative sources:\n\n```console\n$ # Specify a version constraint\n$ uv add 'requests==2.31.0'\n\n$ # Add a git dependency\n$ uv add git+https://github.com/psf/re"], "retrieved": []}
{"id": "q0109", "q": "What does the `--upgrade-package` flag do in `uv lock`?", "shape": "exact_lookup", "gold": ["guides/projects.md#7"], "gold_text": ["## Managing dependencies\n\nYou can add dependencies to your `pyproject.toml` with the `uv add` command. This will also update\nthe lockfile and project environment:\n\n```console\n$ uv add requests\n```\n\nYou can also specify version constraints or alternative sources:\n\n```console\n$ # Specify a version constraint\n$ uv add 'requests==2.31.0'\n\n$ # Add a git dependency\n$ uv add git+https://github.com/psf/re"], "retrieved": []}
{"id": "q0110", "q": "How do you add dependencies from a requirements.txt file to a project?", "shape": "conceptual", "gold": ["concepts/projects/dependencies.md#2"], "gold_text": ["### Importing dependencies from requirements files\n\nDependencies declared in a `requirements.txt` file can be added to the project with the `-r` option:\n\n```\nuv add -r requirements.txt\n```\n\nSee the [pip migration guide](../../guides/migration/pip-to-project.md#importing-requirements-files)\nfor more details."], "retrieved": []}
{"id": "q0111", "q": "How does uv handle a URL dependency introduced transitively by a registry package?", "shape": "conceptual", "gold": ["pip/compatibility.md#6"], "gold_text": ["## Transitive URL dependencies\n\nWhile uv includes first-class support for URL dependencies (e.g., `ruff @ https://...`), it differs\nfrom pip in its handling of _transitive_ URL dependencies in two ways.\n\nFirst, uv makes the assumption that non-URL dependencies do not introduce URL dependencies into the\nresolution. In other words, it assumes that dependencies fetched from a registry do not themselv"], "retrieved": ["concepts/resolution.md#?"]}
{"id": "q0112", "q": "What does the `--constraint` option affect when it is defined with a direct URL dependency?", "shape": "exact_lookup", "gold": ["pip/compatibility.md#6"], "gold_text": ["## Transitive URL dependencies\n\nWhile uv includes first-class support for URL dependencies (e.g., `ruff @ https://...`), it differs\nfrom pip in its handling of _transitive_ URL dependencies in two ways.\n\nFirst, uv makes the assumption that non-URL dependencies do not introduce URL dependencies into the\nresolution. In other words, it assumes that dependencies fetched from a registry do not themselv"], "retrieved": []}
{"id": "q0113", "q": "How do you install a specific Python version or multiple versions with uv?", "shape": "conceptual", "gold": ["guides/install-python.md#2"], "gold_text": ["## Installing a specific version\n\nTo install a specific Python version:\n\n```console\n$ uv python install 3.12\n```\n\nTo install multiple Python versions:\n\n```console\n$ uv python install 3.11 3.12\n```\n\nTo install an alternative Python implementation, e.g., PyPy:\n\n```console\n$ uv python install pypy@3.10\n```\n\nSee the [`python install`](../concepts/python-versions.md#installing-a-python-version) documen"], "retrieved": []}
{"id": "q0114", "q": "What does `uv python install` do in this example?", "shape": "exact_lookup", "gold": ["guides/install-python.md#2"], "gold_text": ["## Installing a specific version\n\nTo install a specific Python version:\n\n```console\n$ uv python install 3.12\n```\n\nTo install multiple Python versions:\n\n```console\n$ uv python install 3.11 3.12\n```\n\nTo install an alternative Python implementation, e.g., PyPy:\n\n```console\n$ uv python install pypy@3.10\n```\n\nSee the [`python install`](../concepts/python-versions.md#installing-a-python-version) documen"], "retrieved": []}
{"id": "q0115", "q": "How do you install a package from a GitHub branch or tag with uv pip?", "shape": "conceptual", "gold": ["pip/packages.md#0"], "gold_text": ["# Managing packages\n\n## Installing a package\n\nTo install a package into the virtual environment, e.g., Flask:\n\n```console\n$ uv pip install flask\n```\n\nTo install a package with optional dependencies enabled, e.g., Flask with the \"dotenv\" extra:\n\n```console\n$ uv pip install \"flask[dotenv]\"\n```\n\nTo install multiple packages, e.g., Flask and Ruff:\n\n```console\n$ uv pip install flask ruff\n```\n\nTo instal"], "retrieved": []}
{"id": "q0116", "q": "What does 'ruff==0.3.0' mean when installing with uv pip?", "shape": "exact_lookup", "gold": ["pip/packages.md#0"], "gold_text": ["# Managing packages\n\n## Installing a package\n\nTo install a package into the virtual environment, e.g., Flask:\n\n```console\n$ uv pip install flask\n```\n\nTo install a package with optional dependencies enabled, e.g., Flask with the \"dotenv\" extra:\n\n```console\n$ uv pip install \"flask[dotenv]\"\n```\n\nTo install multiple packages, e.g., Flask and Ruff:\n\n```console\n$ uv pip install flask ruff\n```\n\nTo instal"], "retrieved": []}
{"id": "q0117", "q": "What does uv do for scripts that include inline metadata?", "shape": "conceptual", "gold": ["reference/storage.md#14"], "gold_text": ["### Script virtual environments\n\nWhen running [scripts with inline metadata](../guides/scripts.md), uv creates a dedicated virtual\nenvironment for each script in the [cache directory](#cache-directory)."], "retrieved": []}
{"id": "q0118", "q": "What is the difference between locking and syncing in a project's dependency workflow?", "shape": "conceptual", "gold": ["concepts/projects/sync.md#0"], "gold_text": ["# Locking and syncing\n\nLocking is the process of [resolving](../resolution.md) your project's dependencies into a\n[lockfile](./layout.md#the-lockfile). Syncing is the process of installing a subset of packages from\nthe lockfile into the [project environment](./layout.md#the-project-environment)."], "retrieved": ["concepts/projects/sync.md#1"]}
{"id": "q0119", "q": "How do you install a project and run commands in a GitHub Actions environment with uv?", "shape": "conceptual", "gold": ["guides/integration/github.md#4"], "gold_text": ["## Syncing and running\n\nOnce uv and Python are installed, the project can be installed with `uv sync` and commands can be\nrun in the environment with `uv run`:\n\n```yaml title=\"example.yml\" hl_lines=\"15 17-22\"\nname: Example\n\njobs:\n  uv-example:\n    name: python\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n\n      - name: Inst"], "retrieved": ["guides/integration/github.md#1"]}
{"id": "q0120", "q": "What does the `UV_PROJECT_ENVIRONMENT` setting change?", "shape": "exact_lookup", "gold": ["guides/integration/github.md#4"], "gold_text": ["## Syncing and running\n\nOnce uv and Python are installed, the project can be installed with `uv sync` and commands can be\nrun in the environment with `uv run`:\n\n```yaml title=\"example.yml\" hl_lines=\"15 17-22\"\nname: Example\n\njobs:\n  uv-example:\n    name: python\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n\n      - name: Inst"], "retrieved": []}
{"id": "q0121", "q": "How are project, user, and system uv configuration files combined when more than one is found?", "shape": "conceptual", "gold": ["concepts/configuration-files.md#1"], "gold_text": ["uv will also discover `uv.toml` configuration files in the user- and system-level\n[configuration directories](../reference/storage.md#configuration-directories), e.g., user-level\nconfiguration in `~/.config/uv/uv.toml` on macOS and Linux, or `%APPDATA%\\uv\\uv.toml` on Windows,\nand system-level configuration at `/etc/uv/uv.toml` on macOS and Linux, or\n`%PROGRAMDATA%\\uv\\uv.toml` on Windows.\n\n!!! impo"], "retrieved": []}
{"id": "q0122", "q": "What does the --no-config command-line argument do in uv?", "shape": "exact_lookup", "gold": ["concepts/configuration-files.md#1"], "gold_text": ["uv will also discover `uv.toml` configuration files in the user- and system-level\n[configuration directories](../reference/storage.md#configuration-directories), e.g., user-level\nconfiguration in `~/.config/uv/uv.toml` on macOS and Linux, or `%APPDATA%\\uv\\uv.toml` on Windows,\nand system-level configuration at `/etc/uv/uv.toml` on macOS and Linux, or\n`%PROGRAMDATA%\\uv\\uv.toml` on Windows.\n\n!!! impo"], "retrieved": []}
{"id": "q0123", "q": "How can uv help when deploying Python code to AWS Lambda?", "shape": "conceptual", "gold": ["guides/integration/aws-lambda.md#0"], "gold_text": ["# Using uv with AWS Lambda\n\n[AWS Lambda](https://aws.amazon.com/lambda/) is a serverless computing service that lets you run\ncode without provisioning or managing servers.\n\nYou can use uv with AWS Lambda to manage your Python dependencies, build your deployment package,\nand deploy your Lambda functions.\n\n!!! tip\n\nCheck out the [`uv-aws-lambda-example`](https://github.com/astral-sh/uv-aws-lambda-ex"], "retrieved": ["guides/integration/aws-lambda.md#1"]}
{"id": "q0124", "q": "How do tool upgrades handle existing version constraints when a tool was originally installed with a version range?", "shape": "conceptual", "gold": ["guides/tools.md#9"], "gold_text": ["## Upgrading tools\n\nTo upgrade a tool, use `uv tool upgrade`:\n\n```console\n$ uv tool upgrade ruff\n```\n\nTool upgrades will respect the version constraints provided when installing the tool. For example,\n`uv tool install ruff >=0.3,<0.4` followed by `uv tool upgrade ruff` will upgrade Ruff to the latest\nversion in the range `>=0.3,<0.4`.\n\nTo instead replace the version constraints, re-install the too"], "retrieved": []}
{"id": "q0125", "q": "What does `uv tool upgrade --all` do?", "shape": "exact_lookup", "gold": ["guides/tools.md#9"], "gold_text": ["## Upgrading tools\n\nTo upgrade a tool, use `uv tool upgrade`:\n\n```console\n$ uv tool upgrade ruff\n```\n\nTool upgrades will respect the version constraints provided when installing the tool. For example,\n`uv tool install ruff >=0.3,<0.4` followed by `uv tool upgrade ruff` will upgrade Ruff to the latest\nversion in the range `>=0.3,<0.4`.\n\nTo instead replace the version constraints, re-install the too"], "retrieved": ["guides/tools.md#1"]}
{"id": "q0126", "q": "When should dependency overrides be used instead of constraints in uv?", "shape": "conceptual", "gold": ["concepts/resolution.md#17"], "gold_text": ["## Dependency overrides\n\nDependency overrides allow bypassing unsuccessful or undesirable resolutions by overriding a\npackage's declared dependencies. Overrides are a useful last resort for cases in which you _know_\nthat a dependency is compatible with a certain version of a package, despite the metadata indicating\notherwise.\n\nFor example, if a transitive dependency declares the requirement `pydan"], "retrieved": []}
{"id": "q0127", "q": "What does `tool.uv.override-dependencies` configure in pyproject.toml?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#17"], "gold_text": ["## Dependency overrides\n\nDependency overrides allow bypassing unsuccessful or undesirable resolutions by overriding a\npackage's declared dependencies. Overrides are a useful last resort for cases in which you _know_\nthat a dependency is compatible with a certain version of a package, despite the metadata indicating\notherwise.\n\nFor example, if a transitive dependency declares the requirement `pydan"], "retrieved": ["concepts/projects/config.md#1"]}
{"id": "q0128", "q": "How does uv handle source distribution archives that don't match PEP 625 extension requirements?", "shape": "conceptual", "gold": ["concepts/resolution.md#29"], "gold_text": ["## Source distribution\n\n[PEP 625](https://peps.python.org/pep-0625/) specifies that packages must distribute source\ndistributions as gzip tarball (`.tar.gz`) archives. Prior to this specification, other archive\nformats, which need to be supported for backward compatibility, were also allowed.\n\n!!! important\n\nAs of 0.12, uv rejects source distributions that do not confirm to\n    [PEP 625]'s extensi"], "retrieved": []}
{"id": "q0129", "q": "What does PEP 625 require for source distribution archive formats?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#29"], "gold_text": ["## Source distribution\n\n[PEP 625](https://peps.python.org/pep-0625/) specifies that packages must distribute source\ndistributions as gzip tarball (`.tar.gz`) archives. Prior to this specification, other archive\nformats, which need to be supported for backward compatibility, were also allowed.\n\n!!! important\n\nAs of 0.12, uv rejects source distributions that do not confirm to\n    [PEP 625]'s extensi"], "retrieved": []}
{"id": "q0130", "q": "How can uv be used with a FastAPI project?", "shape": "conceptual", "gold": ["guides/integration/fastapi.md#0"], "gold_text": ["# Using uv with FastAPI\n\n[FastAPI](https://github.com/fastapi/fastapi) is a modern, high-performance Python web framework.\nYou can use uv to manage your FastAPI project, including installing dependencies, managing\nenvironments, running FastAPI applications, and more.\n\n!!! note\n\nYou can view the source code for this guide in the [uv-fastapi-example](https://github.com/astral-sh/uv-fastapi-example) "], "retrieved": ["guides/integration/fastapi.md#1"]}
{"id": "q0131", "q": "How does uv choose which Python versions to install when a project has a .python-version or .python-versions file?", "shape": "conceptual", "gold": ["concepts/python-versions.md#4"], "gold_text": ["## Installing a Python version\n\nuv bundles a list of downloadable CPython and PyPy distributions for macOS, Linux, and Windows.\n\n!!! tip\n\nBy default, Python versions are automatically downloaded as needed without using\n    `uv python install`.\n\nTo install a Python version at a specific version:\n\n```console\n$ uv python install 3.12.3\n```\n\nTo install the latest patch version:\n\n```console\n$ uv python"], "retrieved": []}
{"id": "q0132", "q": "What does a .python-versions file do for uv python install?", "shape": "exact_lookup", "gold": ["concepts/python-versions.md#4"], "gold_text": ["## Installing a Python version\n\nuv bundles a list of downloadable CPython and PyPy distributions for macOS, Linux, and Windows.\n\n!!! tip\n\nBy default, Python versions are automatically downloaded as needed without using\n    `uv python install`.\n\nTo install a Python version at a specific version:\n\n```console\n$ uv python install 3.12.3\n```\n\nTo install the latest patch version:\n\n```console\n$ uv python"], "retrieved": ["concepts/python-versions.md#1"]}
{"id": "q0133", "q": "How does uv choose between system Python and managed Python installations by default?", "shape": "conceptual", "gold": ["concepts/python-versions.md#17"], "gold_text": ["## Adjusting Python version preferences\n\nThe [`python-preference`](../reference/settings.md#python-preference) setting determines whether to\nprefer using Python installations that are already present on the system, or those that are\ndownloaded and installed by uv.\n\nBy default, the `python-preference` is set to `managed` which prefers managed Python installations\nover system Python installations. H"], "retrieved": ["concepts/python-versions.md#1"]}
{"id": "q0134", "q": "What does the python-preference setting control?", "shape": "exact_lookup", "gold": ["concepts/python-versions.md#17"], "gold_text": ["## Adjusting Python version preferences\n\nThe [`python-preference`](../reference/settings.md#python-preference) setting determines whether to\nprefer using Python installations that are already present on the system, or those that are\ndownloaded and installed by uv.\n\nBy default, the `python-preference` is set to `managed` which prefers managed Python installations\nover system Python installations. H"], "retrieved": []}
{"id": "q0135", "q": "How do you traverse a script's dependency graph while avoiding cycles and skipping platform-incompatible dependencies?", "shape": "conceptual", "gold": ["reference/internals/metadata.md#4"], "gold_text": ["For a script, start from its resolution node in exactly the same way:\n\n```python\nscript_node = metadata.resolution[metadata.script.id]\nvisit(metadata, [script_node])\n```\n\nWhere `visit` is your favourite graph traversal algorithm like depth-first-search:\n\n```python\ndef visit(metadata: UvMetadata, to_analyze: list[Node]):\n    visited = set()\n    while len(to_analyze) > 0:\n        node = to_analyze.p"], "retrieved": []}
{"id": "q0136", "q": "What does `node.kind` indicate when it equals `\"package\"`?", "shape": "exact_lookup", "gold": ["reference/internals/metadata.md#4"], "gold_text": ["For a script, start from its resolution node in exactly the same way:\n\n```python\nscript_node = metadata.resolution[metadata.script.id]\nvisit(metadata, [script_node])\n```\n\nWhere `visit` is your favourite graph traversal algorithm like depth-first-search:\n\n```python\ndef visit(metadata: UvMetadata, to_analyze: list[Node]):\n    visited = set()\n    while len(to_analyze) > 0:\n        node = to_analyze.p"], "retrieved": []}
{"id": "q0137", "q": "How can uv be installed to a fixed path in CI without changing shell profiles or environment variables?", "shape": "conceptual", "gold": ["reference/installer.md#2"], "gold_text": ["## Unmanaged installations\n\nIn ephemeral environments like CI, use `UV_UNMANAGED_INSTALL` to install uv to a specific path while\npreventing the installer from modifying shell profiles or environment variables:\n\n```console\n$ curl -LsSf https://astral.sh/uv/install.sh | env UV_UNMANAGED_INSTALL=\"/custom/path\" sh\n```\n\nThe use of `UV_UNMANAGED_INSTALL` will also disable self-updates (via `uv self upda"], "retrieved": ["reference/installer.md#1"]}
{"id": "q0138", "q": "What does the UV_UNMANAGED_INSTALL environment variable do?", "shape": "exact_lookup", "gold": ["reference/installer.md#2"], "gold_text": ["## Unmanaged installations\n\nIn ephemeral environments like CI, use `UV_UNMANAGED_INSTALL` to install uv to a specific path while\npreventing the installer from modifying shell profiles or environment variables:\n\n```console\n$ curl -LsSf https://astral.sh/uv/install.sh | env UV_UNMANAGED_INSTALL=\"/custom/path\" sh\n```\n\nThe use of `UV_UNMANAGED_INSTALL` will also disable self-updates (via `uv self upda"], "retrieved": []}
{"id": "q0139", "q": "What happens when uv ignores an index error code during resolution?", "shape": "conceptual", "gold": ["concepts/indexes.md#7"], "gold_text": ["### Ignoring error codes\n\nWhen using the [first-index strategy](#searching-across-multiple-indexes), uv will stop searching\nacross indexes if an HTTP 401 Unauthorized or HTTP 403 Forbidden status code is encountered. The one\nexception is that uv will ignore 403s when searching the `pytorch` index (since this index returns a\n403 when a package is not present).\n\nBy default, uv will also stop resolut"], "retrieved": []}
{"id": "q0140", "q": "What does the ignore-error-codes setting control for an index?", "shape": "exact_lookup", "gold": ["concepts/indexes.md#7"], "gold_text": ["### Ignoring error codes\n\nWhen using the [first-index strategy](#searching-across-multiple-indexes), uv will stop searching\nacross indexes if an HTTP 401 Unauthorized or HTTP 403 Forbidden status code is encountered. The one\nexception is that uv will ignore 403s when searching the `pytorch` index (since this index returns a\n403 when a package is not present).\n\nBy default, uv will also stop resolut"], "retrieved": ["concepts/indexes.md#1"]}
{"id": "q0141", "q": "Why should the project directory be mounted into a container during development?", "shape": "conceptual", "gold": ["guides/integration/docker.md#7"], "gold_text": ["## Developing in a container\n\nWhen developing, it's useful to mount the project directory into a container. With this setup,\nchanges to the project can be immediately reflected in a containerized service without rebuilding\nthe image. However, it is important _not_ to include the project virtual environment (`.venv`) in\nthe mount, because the virtual environment is platform specific and the one bui"], "retrieved": []}
{"id": "q0142", "q": "How can uv help manage PyTorch projects across different Python versions and accelerator choices?", "shape": "conceptual", "gold": ["guides/integration/pytorch.md#0"], "gold_text": ["# Using uv with PyTorch\n\nThe [PyTorch](https://pytorch.org/) ecosystem is a popular choice for deep learning research and\ndevelopment. You can use uv to manage PyTorch projects and PyTorch dependencies across different\nPython versions and environments, even controlling for the choice of accelerator (e.g., CPU-only vs.\nCUDA).\n\n!!! note\n\nSome of the features outlined in this guide require uv version"], "retrieved": []}
{"id": "q0143", "q": "How does uv decide whether to build and install the current project or only its dependencies?", "shape": "conceptual", "gold": ["concepts/projects/config.md#5"], "gold_text": ["## Build systems\n\nA build system determines how the project should be packaged and installed. Projects may declare and\nconfigure a build system in the `[build-system]` table of the `pyproject.toml`.\n\nuv uses the presence of a build system to determine if a project contains a package that should be\ninstalled in the project virtual environment. If a build system is not defined, uv will not attempt\nt"], "retrieved": []}
{"id": "q0144", "q": "Why use a separate Lambda layer for dependencies instead of bundling everything with the application code?", "shape": "conceptual", "gold": ["guides/integration/aws-lambda.md#13"], "gold_text": ["### Using a Lambda layer\n\nAWS Lambda also supports the deployment of multiple composed\n[Lambda layers](https://docs.aws.amazon.com/lambda/latest/dg/python-layers.html) when working with\nzip archives. These layers are conceptually similar to layers in a Docker image, allowing you to\nseparate application code from dependencies.\n\nIn particular, we can create a lambda layer for application dependencie"], "retrieved": []}
{"id": "q0145", "q": "What does the `--prefix` option do in the dependency install command?", "shape": "exact_lookup", "gold": ["guides/integration/aws-lambda.md#13"], "gold_text": ["### Using a Lambda layer\n\nAWS Lambda also supports the deployment of multiple composed\n[Lambda layers](https://docs.aws.amazon.com/lambda/latest/dg/python-layers.html) when working with\nzip archives. These layers are conceptually similar to layers in a Docker image, allowing you to\nseparate application code from dependencies.\n\nIn particular, we can create a lambda layer for application dependencie"], "retrieved": []}
{"id": "q0146", "q": "Why can preview features sometimes be used without changing preview settings?", "shape": "conceptual", "gold": ["concepts/preview.md#2"], "gold_text": ["## Using preview features\n\nOften, preview features can be used without changing any preview settings if the behavior change is\ngated by some sort of user interaction, For example, while `pylock.toml` support is in preview, you\ncan use `uv pip install` with a `pylock.toml` file without additional configuration because\nspecifying the `pylock.toml` file indicates you want to use the feature. However,"], "retrieved": ["concepts/preview.md#1"]}
{"id": "q0147", "q": "What does using `pylock.toml` with `uv pip install` indicate?", "shape": "exact_lookup", "gold": ["concepts/preview.md#2"], "gold_text": ["## Using preview features\n\nOften, preview features can be used without changing any preview settings if the behavior change is\ngated by some sort of user interaction, For example, while `pylock.toml` support is in preview, you\ncan use `uv pip install` with a `pylock.toml` file without additional configuration because\nspecifying the `pylock.toml` file indicates you want to use the feature. However,"], "retrieved": ["pip/compatibility.md#1"]}
{"id": "q0148", "q": "Why is a Docker image recommended for a reproducible example?", "shape": "conceptual", "gold": ["reference/troubleshooting/reproducible-examples.md#2"], "gold_text": ["## Strategies for reproducible examples\n\n### Docker image\n\nWriting a Docker image is often the best way to share a reproducible example because it is entirely\nself-contained. This means that the state from the reproducer's system does not affect the problem.\n\n!!! note\n\nUsing a Docker image is only feasible if the issue is reproducible on Linux. When using macOS,\n    it's prudent to ensure your ima"], "retrieved": ["reference/troubleshooting/reproducible-examples.md#1"]}
{"id": "q0149", "q": "What does `linux/amd64` specify in the Dockerfile example?", "shape": "exact_lookup", "gold": ["reference/troubleshooting/reproducible-examples.md#2"], "gold_text": ["## Strategies for reproducible examples\n\n### Docker image\n\nWriting a Docker image is often the best way to share a reproducible example because it is entirely\nself-contained. This means that the state from the reproducer's system does not affect the problem.\n\n!!! note\n\nUsing a Docker image is only feasible if the issue is reproducible on Linux. When using macOS,\n    it's prudent to ensure your ima"], "retrieved": []}
{"id": "q0150", "q": "How do you publish a package to a custom index configured in `[[tool.uv.index]]`?", "shape": "conceptual", "gold": ["guides/package.md#5"], "gold_text": ["## Publishing your package\n\n!!! note\n\nA complete guide to publishing from GitHub Actions to PyPI can be found in the\n    [GitHub Guide](integration/github.md#publishing-to-pypi)\n\nPublish your package with `uv publish`:\n\n```console\n$ uv publish\n```\n\nSet a PyPI token with `--token` or `UV_PUBLISH_TOKEN`, or set a username with `--username` or\n`UV_PUBLISH_USERNAME` and password with `--password` or `"], "retrieved": []}
{"id": "q0151", "q": "What does `UV_PUBLISH_TOKEN` control when publishing with uv?", "shape": "exact_lookup", "gold": ["guides/package.md#5"], "gold_text": ["## Publishing your package\n\n!!! note\n\nA complete guide to publishing from GitHub Actions to PyPI can be found in the\n    [GitHub Guide](integration/github.md#publishing-to-pypi)\n\nPublish your package with `uv publish`:\n\n```console\n$ uv publish\n```\n\nSet a PyPI token with `--token` or `UV_PUBLISH_TOKEN`, or set a username with `--username` or\n`UV_PUBLISH_USERNAME` and password with `--password` or `"], "retrieved": []}
{"id": "q0152", "q": "How can GitHub Actions install the Python version pinned by a project when setting up Python?", "shape": "conceptual", "gold": ["guides/integration/github.md#1"], "gold_text": ["## Setting up Python\n\nPython can be installed with the `python install` command:\n\n```yaml title=\"example.yml\" hl_lines=\"14 15\"\nname: Example\n\njobs:\n  uv-example:\n    name: python\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n\n      - name: Install uv\n        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 #"], "retrieved": []}
{"id": "q0153", "q": "What does the python-version-file option control in setup-python?", "shape": "exact_lookup", "gold": ["guides/integration/github.md#1"], "gold_text": ["## Setting up Python\n\nPython can be installed with the `python install` command:\n\n```yaml title=\"example.yml\" hl_lines=\"14 15\"\nname: Example\n\njobs:\n  uv-example:\n    name: python\n    runs-on: ubuntu-latest\n\n    steps:\n      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1\n\n      - name: Install uv\n        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 #"], "retrieved": []}
{"id": "q0154", "q": "When should tool.uv.dependency-metadata be used instead of dependency overrides?", "shape": "conceptual", "gold": ["concepts/resolution.md#21"], "gold_text": ["```toml\n[project]\nname = \"project\"\nversion = \"0.1.0\"\nrequires-python = \">=3.12\"\ndependencies = [\"flash-attn\"]\n\n[tool.uv.sources]\nflash-attn = { git = \"https://github.com/Dao-AILab/flash-attention\", tag = \"v2.6.3\" }\n\n[[tool.uv.dependency-metadata]]\nname = \"flash-attn\"\nversion = \"2.6.3\"\nrequires-dist = [\"torch\", \"einops\"]\n```\n\nLike dependency overrides, `tool.uv.dependency-metadata` can also be used"], "retrieved": []}
{"id": "q0155", "q": "How do pip and uv treat direct URL dependencies when --only-binary :all: is set?", "shape": "conceptual", "gold": ["pip/compatibility.md#12"], "gold_text": ["## `--only-binary` enforcement\n\nThe `--only-binary` argument is used to restrict installation to pre-built binary distributions.\nWhen `--only-binary :all:` is provided, both pip and uv will refuse to build source distributions\nfrom PyPI and other registries.\n\nHowever, when a dependency is provided as a direct URL (e.g., `uv pip install https://...`), pip\ndoes _not_ enforce `--only-binary`, and wil"], "retrieved": []}
{"id": "q0156", "q": "What does --only-binary :all: do during installation?", "shape": "exact_lookup", "gold": ["pip/compatibility.md#12"], "gold_text": ["## `--only-binary` enforcement\n\nThe `--only-binary` argument is used to restrict installation to pre-built binary distributions.\nWhen `--only-binary :all:` is provided, both pip and uv will refuse to build source distributions\nfrom PyPI and other registries.\n\nHowever, when a dependency is provided as a direct URL (e.g., `uv pip install https://...`), pip\ndoes _not_ enforce `--only-binary`, and wil"], "retrieved": ["pip/compatibility.md#1"]}
{"id": "q0157", "q": "How are package indexes prioritized when uv resolves dependencies?", "shape": "conceptual", "gold": ["concepts/indexes.md#1"], "gold_text": ["## Defining an index\n\nTo include an additional index when resolving dependencies, add a `[[tool.uv.index]]` entry to your\n`pyproject.toml`:\n\n```toml\n[[tool.uv.index]]\n# Optional name for the index.\nname = \"pytorch\"\n# Required URL for the index.\nurl = \"https://download.pytorch.org/whl/cpu\"\n```\n\nIndexes are prioritized in the order in which they’re defined, such that the first index listed in\nthe co"], "retrieved": []}
{"id": "q0159", "q": "How can a specific package index use a different cutoff for reproducible resolution filtering?", "shape": "conceptual", "gold": ["concepts/indexes.md#11"], "gold_text": ["### Configuring `exclude-newer` for an index\n\nIf you're using [`exclude-newer`](./resolution.md#reproducible-resolutions), you can configure a\ndifferent cutoff for a specific index:\n\n```toml\n[[tool.uv.index]]\nname = \"internal\"\nurl = \"https://internal.example.com/simple\"\nexclude-newer = \"7 days\"\n```\n\nIndex-specific values only affect packages served from that index. Package-specific\n`exclude-newer-"], "retrieved": []}
{"id": "q0160", "q": "What does `exclude-newer` do for a specific index?", "shape": "exact_lookup", "gold": ["concepts/indexes.md#11"], "gold_text": ["### Configuring `exclude-newer` for an index\n\nIf you're using [`exclude-newer`](./resolution.md#reproducible-resolutions), you can configure a\ndifferent cutoff for a specific index:\n\n```toml\n[[tool.uv.index]]\nname = \"internal\"\nurl = \"https://internal.example.com/simple\"\nexclude-newer = \"7 days\"\n```\n\nIndex-specific values only affect packages served from that index. Package-specific\n`exclude-newer-"], "retrieved": []}
{"id": "q0161", "q": "How can an application project be created instead of the default project type with uv init?", "shape": "conceptual", "gold": ["concepts/projects/init.md#2"], "gold_text": ["## Applications\n\nApplication projects are suitable for web servers, scripts, and command-line interfaces.\n\nApplications are the default target for `uv init`, but can also be specified with the `--app` flag:\n\n```console\n$ uv init example-app\n```\n\nThe source code lives in a `src` directory with a module directory and an `__init__.py` file:\n\n```console\n$ tree example-app\nexample-app/\n├── .python-vers"], "retrieved": []}
{"id": "q0162", "q": "What does the `--build-backend` option control?", "shape": "exact_lookup", "gold": ["concepts/projects/init.md#2"], "gold_text": ["## Applications\n\nApplication projects are suitable for web servers, scripts, and command-line interfaces.\n\nApplications are the default target for `uv init`, but can also be specified with the `--app` flag:\n\n```console\n$ uv init example-app\n```\n\nThe source code lives in a `src` directory with a module directory and an `__init__.py` file:\n\n```console\n$ tree example-app\nexample-app/\n├── .python-vers"], "retrieved": []}
{"id": "q0163", "q": "What should the first step of a reproduction script do for a Git repository issue?", "shape": "conceptual", "gold": ["reference/troubleshooting/reproducible-examples.md#5"], "gold_text": ["### Git repository\n\nWhen sharing a Git repository reproduction, include a [script](#script) that reproduces the problem\nor, even better, a [Dockerfile](#docker-image). The first step of the script should be to clone the\nrepository and checkout a specific commit:\n\n```console\n$ git clone https://github.com/<user>/<project>.git\n$ cd <project>\n$ git checkout <commit>\n$ <commands to produce error>\n```\n"], "retrieved": ["reference/troubleshooting/reproducible-examples.md#1"]}
{"id": "q0164", "q": "What does `gh repo create uv-mre-1234 --clone` do?", "shape": "exact_lookup", "gold": ["reference/troubleshooting/reproducible-examples.md#5"], "gold_text": ["### Git repository\n\nWhen sharing a Git repository reproduction, include a [script](#script) that reproduces the problem\nor, even better, a [Dockerfile](#docker-image). The first step of the script should be to clone the\nrepository and checkout a specific commit:\n\n```console\n$ git clone https://github.com/<user>/<project>.git\n$ cd <project>\n$ git checkout <commit>\n$ <commands to produce error>\n```\n"], "retrieved": []}
{"id": "q0165", "q": "How does uv choose packages when multiple package indexes are configured?", "shape": "conceptual", "gold": ["pip/compatibility.md#4"], "gold_text": ["As of v0.1.39, users can opt in to `pip`-style behavior for multiple indexes via the\n`--index-strategy` command-line option, or the `UV_INDEX_STRATEGY` environment variable, which\nsupports the following values:\n\n- `first-index` (default): Search for each package across all indexes, limiting the candidate\n  versions to those present in the first index that contains the package, prioritizing the\n  `"], "retrieved": ["concepts/indexes.md#1"]}
{"id": "q0166", "q": "What does the UV_INDEX_STRATEGY environment variable control?", "shape": "exact_lookup", "gold": ["pip/compatibility.md#4"], "gold_text": ["As of v0.1.39, users can opt in to `pip`-style behavior for multiple indexes via the\n`--index-strategy` command-line option, or the `UV_INDEX_STRATEGY` environment variable, which\nsupports the following values:\n\n- `first-index` (default): Search for each package across all indexes, limiting the candidate\n  versions to those present in the first index that contains the package, prioritizing the\n  `"], "retrieved": []}
{"id": "q0167", "q": "Why would changing build-related settings cause some installed packages to be rebuilt?", "shape": "conceptual", "gold": ["concepts/projects/config.md#12"], "gold_text": ["The use of `extra-build-dependencies` and `extra-build-variables` are tracked in the uv cache, such\nthat changes to these settings will trigger a reinstall and rebuild of the affected packages. For\nexample, in the case of `flash-attn`, upgrading the version of `torch` used in your project would\nsubsequently trigger a rebuild of `flash-attn` with the new version of `torch`."], "retrieved": []}
{"id": "q0168", "q": "What do `extra-build-dependencies` and `extra-build-variables` affect in the uv cache?", "shape": "exact_lookup", "gold": ["concepts/projects/config.md#12"], "gold_text": ["The use of `extra-build-dependencies` and `extra-build-variables` are tracked in the uv cache, such\nthat changes to these settings will trigger a reinstall and rebuild of the affected packages. For\nexample, in the case of `flash-attn`, upgrading the version of `torch` used in your project would\nsubsequently trigger a rebuild of `flash-attn` with the new version of `torch`."], "retrieved": []}
{"id": "q0169", "q": "How do you update a Lambda function when only the dependency layer changes?", "shape": "conceptual", "gold": ["guides/integration/aws-lambda.md#14"], "gold_text": ["```console\n$ # Zip the application code.\n$ zip -r app.zip app\n\n$ # Create the Lambda function.\n$ aws lambda create-function \\\n   --function-name myFunction \\\n   --runtime python3.13 \\\n   --zip-file fileb://app.zip \\\n   --handler app.main.handler \\\n   --role arn:aws:iam::111122223333:role/service-role/my-lambda-role\n```\n\nFinally, we can attach the dependencies layer to the Lambda function, using th"], "retrieved": ["guides/integration/aws-lambda.md#1"]}
{"id": "q0170", "q": "What does the --cli-binary-format raw-in-base64-out option do in the Lambda update command?", "shape": "exact_lookup", "gold": ["guides/integration/aws-lambda.md#14"], "gold_text": ["```console\n$ # Zip the application code.\n$ zip -r app.zip app\n\n$ # Create the Lambda function.\n$ aws lambda create-function \\\n   --function-name myFunction \\\n   --runtime python3.13 \\\n   --zip-file fileb://app.zip \\\n   --handler app.main.handler \\\n   --role arn:aws:iam::111122223333:role/service-role/my-lambda-role\n```\n\nFinally, we can attach the dependencies layer to the Lambda function, using th"], "retrieved": []}
{"id": "q0171", "q": "What level of support does uv give to Tier 2 platforms, and how well are they tested?", "shape": "conceptual", "gold": ["reference/policies/platforms.md#0"], "gold_text": ["# Platform support\n\nuv has Tier 1 support for the following platforms:\n\n- macOS (Apple Silicon)\n- macOS (x86_64)\n- Linux (x86_64)\n- Windows (x86_64)\n\nuv is continuously built, tested, and developed against its Tier 1 platforms. Inspired by the Rust\nproject, Tier 1 can be thought of as\n[\"guaranteed to work\"](https://doc.rust-lang.org/beta/rustc/platform-support.html#tier-1).\n\nuv has Tier 2 support\n"], "retrieved": []}
{"id": "q0172", "q": "What does Tier 3 support mean for uv platforms?", "shape": "exact_lookup", "gold": ["reference/policies/platforms.md#0"], "gold_text": ["# Platform support\n\nuv has Tier 1 support for the following platforms:\n\n- macOS (Apple Silicon)\n- macOS (x86_64)\n- Linux (x86_64)\n- Windows (x86_64)\n\nuv is continuously built, tested, and developed against its Tier 1 platforms. Inspired by the Rust\nproject, Tier 1 can be thought of as\n[\"guaranteed to work\"](https://doc.rust-lang.org/beta/rustc/platform-support.html#tier-1).\n\nuv has Tier 2 support\n"], "retrieved": []}
{"id": "q0173", "q": "Why create a dedicated Jupyter kernel for a project when installing packages from a notebook?", "shape": "conceptual", "gold": ["guides/integration/jupyter.md#2"], "gold_text": ["### Creating a kernel\n\nIf you need to install packages from within the notebook, we recommend creating a dedicated kernel\nfor your project. Kernels enable the Jupyter server to run in one environment, with individual\nnotebooks running in their own, separate environments.\n\nIn the context of uv, we can create a kernel for a project while installing Jupyter itself in an\nisolated environment, as in `u"], "retrieved": ["guides/integration/jupyter.md#1"]}
{"id": "q0174", "q": "What does the `--env VIRTUAL_ENV` option do in the kernel install command?", "shape": "exact_lookup", "gold": ["guides/integration/jupyter.md#2"], "gold_text": ["### Creating a kernel\n\nIf you need to install packages from within the notebook, we recommend creating a dedicated kernel\nfor your project. Kernels enable the Jupyter server to run in one environment, with individual\nnotebooks running in their own, separate environments.\n\nIn the context of uv, we can create a kernel for a project while installing Jupyter itself in an\nisolated environment, as in `u"], "retrieved": ["guides/integration/jupyter.md#1"]}
{"id": "q0175", "q": "When does uv use its bundled build backend instead of the separate uv_build package?", "shape": "conceptual", "gold": ["concepts/build-backend.md#3"], "gold_text": ["## Bundled build backend\n\nThe build backend is published as a separate package (`uv_build`) that is optimized for portability\nand small binary size. However, the `uv` executable also includes a copy of the build backend, which\nwill be used during builds performed by uv, e.g., during `uv build`, if its version is compatible\nwith the `uv_build` requirement. If it's not compatible, a compatible versi"], "retrieved": []}
{"id": "q0176", "q": "What is the uv_build package used for?", "shape": "exact_lookup", "gold": ["concepts/build-backend.md#3"], "gold_text": ["## Bundled build backend\n\nThe build backend is published as a separate package (`uv_build`) that is optimized for portability\nand small binary size. However, the `uv` executable also includes a copy of the build backend, which\nwill be used during builds performed by uv, e.g., during `uv build`, if its version is compatible\nwith the `uv_build` requirement. If it's not compatible, a compatible versi"], "retrieved": []}
{"id": "q0178", "q": "What does the `--credential_helper` option do in Bazel 7 and newer?", "shape": "exact_lookup", "gold": ["guides/integration/bazel.md#1"], "gold_text": ["## Authentication\n\nBazel 7 and newer supports credential helpers via the `--credential_helper` option. To use\ncredentials stored by uv for Bazel fetches, first authenticate uv with the service that hosts the\nfiles Bazel needs to fetch:\n\n```console\n$ uv auth login https://packages.example.com\n```\n\nThen, configure Bazel to invoke\n[`uv auth helper`](../../concepts/authentication/cli.md#using-credenti"], "retrieved": ["guides/integration/bazel.md#2"]}
{"id": "q0179", "q": "How do you install a package from a Git branch, tag, or commit with uvx?", "shape": "conceptual", "gold": ["guides/tools.md#5"], "gold_text": ["## Requesting different sources\n\nThe `--from` option can also be used to install from alternative sources.\n\nFor example, to pull from git:\n\n```console\n$ uvx --from git+https://github.com/httpie/cli httpie\n```\n\nYou can also pull the latest commit from a specific named branch:\n\n```console\n$ uvx --from git+https://github.com/httpie/cli@master httpie\n```\n\nOr pull a specific tag:\n\n```console\n$ uvx --fr"], "retrieved": []}
{"id": "q0180", "q": "What does the `--lfs` option do when using `uvx --from` with a Git repository?", "shape": "exact_lookup", "gold": ["guides/tools.md#5"], "gold_text": ["## Requesting different sources\n\nThe `--from` option can also be used to install from alternative sources.\n\nFor example, to pull from git:\n\n```console\n$ uvx --from git+https://github.com/httpie/cli httpie\n```\n\nYou can also pull the latest commit from a specific named branch:\n\n```console\n$ uvx --from git+https://github.com/httpie/cli@master httpie\n```\n\nOr pull a specific tag:\n\n```console\n$ uvx --fr"], "retrieved": []}
{"id": "q0181", "q": "How can you import a requirements.in file into uv while keeping the locked package versions from requirements.txt?", "shape": "conceptual", "gold": ["guides/migration/pip-to-project.md#9"], "gold_text": ["### Importing requirements files\n\nFirst, create a `pyproject.toml` if you have not already:\n\n```console\n$ uv init\n```\n\nThen, the easiest way to import requirements is with `uv add`:\n\n```console\n$ uv add -r requirements.in\n```\n\nHowever, there is some nuance to this transition. Notice we used the `requirements.in` file, which\ndoes not pin to exact versions of packages so uv will solve for new versio"], "retrieved": []}
{"id": "q0182", "q": "What file does uv use to identify the root directory of a Python project?", "shape": "conceptual", "gold": ["concepts/projects/layout.md#0"], "gold_text": ["# Project structure and files\n\n## The `pyproject.toml`\n\nPython project metadata is defined in a\n[`pyproject.toml`](https://packaging.python.org/en/latest/guides/writing-pyproject-toml/) file. uv\nrequires this file to identify the root directory of a project.\n\n!!! tip\n\n`uv init` can be used to create a new project. See [Creating projects](./init.md) for\n    details.\n\nA minimal project definition in"], "retrieved": []}
{"id": "q0183", "q": "How do you run a standalone Python script and manage its dependencies with uv?", "shape": "conceptual", "gold": ["getting-started/features.md#2"], "gold_text": ["## Scripts\n\nExecuting standalone Python scripts, e.g., `example.py`.\n\n- `uv run`: Run a script.\n- `uv add --script`: Add a dependency to a script.\n- `uv remove --script`: Remove a dependency from a script.\n\nSee the [guide on running scripts](../guides/scripts.md) to get started."], "retrieved": ["guides/scripts.md#1"]}
{"id": "q0184", "q": "What does `uv add --script` do?", "shape": "exact_lookup", "gold": ["getting-started/features.md#2"], "gold_text": ["## Scripts\n\nExecuting standalone Python scripts, e.g., `example.py`.\n\n- `uv run`: Run a script.\n- `uv add --script`: Add a dependency to a script.\n- `uv remove --script`: Remove a dependency from a script.\n\nSee the [guide on running scripts](../guides/scripts.md) to get started."], "retrieved": ["guides/scripts.md#1"]}
{"id": "q0185", "q": "Why does uv sometimes build a package during resolution or installation?", "shape": "conceptual", "gold": ["reference/troubleshooting/build-failures.md#5"], "gold_text": ["## Why does uv build a package?\n\nWhen generating the cross-platform lockfile, uv needs to determine the dependencies of all packages,\neven those only installed on other platforms. uv tries to avoid package builds during resolution. It\nuses any wheel if exist for that version, then tries to find static metadata in the source\ndistribution (mainly pyproject.toml with static `project.version`, `projec"], "retrieved": ["concepts/resolution.md#1"]}
{"id": "q0186", "q": "What does project.version refer to in uv's package resolution?", "shape": "exact_lookup", "gold": ["reference/troubleshooting/build-failures.md#5"], "gold_text": ["## Why does uv build a package?\n\nWhen generating the cross-platform lockfile, uv needs to determine the dependencies of all packages,\neven those only installed on other platforms. uv tries to avoid package builds during resolution. It\nuses any wheel if exist for that version, then tries to find static metadata in the source\ndistribution (mainly pyproject.toml with static `project.version`, `projec"], "retrieved": []}
{"id": "q0187", "q": "How is --with-executables-from different from --with when installing a tool?", "shape": "conceptual", "gold": ["concepts/tools.md#7"], "gold_text": ["## Installing executables from additional packages\n\nWhen installing a tool, you may want to include executables from additional packages in the same\ntool environment. This is useful when you have related tools that work together or when you want to\ninstall multiple executables that share dependencies.\n\nThe `--with-executables-from` option allows you to specify additional packages whose executables"], "retrieved": []}
{"id": "q0188", "q": "What does the --with-executables-from option do?", "shape": "exact_lookup", "gold": ["concepts/tools.md#7"], "gold_text": ["## Installing executables from additional packages\n\nWhen installing a tool, you may want to include executables from additional packages in the same\ntool environment. This is useful when you have related tools that work together or when you want to\ninstall multiple executables that share dependencies.\n\nThe `--with-executables-from` option allows you to specify additional packages whose executables"], "retrieved": []}
{"id": "q0189", "q": "How can I make uv require a specific hash algorithm for packages from an index?", "shape": "conceptual", "gold": ["concepts/indexes.md#10"], "gold_text": ["### Requiring a hash algorithm\n\nWhen an index advertises multiple hashes for a distribution, uv selects a single hash to record in\nthe lockfile. To require a specific algorithm for distributions resolved from an index, use the\n`hash-algorithm` setting:\n\n```toml\n[tool.uv]\npreview-features = [\"index-hash-algorithm\"]\n\n[[tool.uv.index]]\nname = \"private-index\"\nurl = \"https://private-index.com/simple\"\nh"], "retrieved": []}
{"id": "q0190", "q": "What does the hash-algorithm setting control in a uv index?", "shape": "exact_lookup", "gold": ["concepts/indexes.md#10"], "gold_text": ["### Requiring a hash algorithm\n\nWhen an index advertises multiple hashes for a distribution, uv selects a single hash to record in\nthe lockfile. To require a specific algorithm for distributions resolved from an index, use the\n`hash-algorithm` setting:\n\n```toml\n[tool.uv]\npreview-features = [\"index-hash-algorithm\"]\n\n[[tool.uv.index]]\nname = \"private-index\"\nurl = \"https://private-index.com/simple\"\nh"], "retrieved": ["concepts/indexes.md#1"]}
{"id": "q0191", "q": "How can uv be used in a Docker build without including it in the final image?", "shape": "conceptual", "gold": ["guides/integration/docker.md#15"], "gold_text": ["### Using uv temporarily\n\nIf uv isn't needed in the final image, the binary can be mounted in each invocation:\n\n```dockerfile title=\"Dockerfile\"\nRUN --mount=from=ghcr.io/astral-sh/uv,source=/uv,target=/bin/uv \\\n    uv sync\n```"], "retrieved": ["guides/integration/docker.md#1"]}
{"id": "q0192", "q": "What does `--mount=from=ghcr.io/astral-sh/uv,source=/uv,target=/bin/uv` do in a Dockerfile?", "shape": "exact_lookup", "gold": ["guides/integration/docker.md#15"], "gold_text": ["### Using uv temporarily\n\nIf uv isn't needed in the final image, the binary can be mounted in each invocation:\n\n```dockerfile title=\"Dockerfile\"\nRUN --mount=from=ghcr.io/astral-sh/uv,source=/uv,target=/bin/uv \\\n    uv sync\n```"], "retrieved": ["guides/integration/docker.md#1"]}
{"id": "q0193", "q": "How do you configure uv to trust custom CA certificates for all requests?", "shape": "conceptual", "gold": ["concepts/authentication/certificates.md#3"], "gold_text": ["## Custom certificates\n\nTo use custom CA certificates, set the\n[`SSL_CERT_FILE`](../../reference/environment.md#ssl_cert_file) environment variable to the path of\na PEM-encoded certificate bundle (e.g., `certs.pem`, `ca-bundle.crt`), or set\n[`SSL_CERT_DIR`](../../reference/environment.md#ssl_cert_dir) to one or more directories containing\nPEM-encoded certificate files. Multiple entries are support"], "retrieved": ["concepts/authentication/certificates.md#1"]}
{"id": "q0194", "q": "What does the SSL_CLIENT_CERT environment variable specify?", "shape": "exact_lookup", "gold": ["concepts/authentication/certificates.md#3"], "gold_text": ["## Custom certificates\n\nTo use custom CA certificates, set the\n[`SSL_CERT_FILE`](../../reference/environment.md#ssl_cert_file) environment variable to the path of\na PEM-encoded certificate bundle (e.g., `certs.pem`, `ca-bundle.crt`), or set\n[`SSL_CERT_DIR`](../../reference/environment.md#ssl_cert_dir) to one or more directories containing\nPEM-encoded certificate files. Multiple entries are support"], "retrieved": ["concepts/authentication/http.md#1"]}
{"id": "q0195", "q": "How can adding lower-bound constraints help when uv tries to build an older package version during resolution", "shape": "conceptual", "gold": ["reference/troubleshooting/build-failures.md#11"], "gold_text": ["### Old version of the package is built\n\nIf a package fails to build during resolution and the version that failed to build is older than the\nversion you want to use, try adding a [constraint](../settings.md#constraint-dependencies) with a\nlower bound (e.g., `numpy>=1.17`). Sometimes, due to algorithmic limitations, the uv resolver tries\nto find a fitting version using unreasonably old packages, w"], "retrieved": []}
{"id": "q0196", "q": "What does the `constraint-dependencies` setting control?", "shape": "exact_lookup", "gold": ["reference/troubleshooting/build-failures.md#11"], "gold_text": ["### Old version of the package is built\n\nIf a package fails to build during resolution and the version that failed to build is older than the\nversion you want to use, try adding a [constraint](../settings.md#constraint-dependencies) with a\nlower bound (e.g., `numpy>=1.17`). Sometimes, due to algorithmic limitations, the uv resolver tries\nto find a fitting version using unreasonably old packages, w"], "retrieved": []}
{"id": "q0197", "q": "How does uv pip differ from uv's primary commands for managing environments?", "shape": "conceptual", "gold": ["pip/index.md#0"], "gold_text": ["# The pip interface\n\nuv provides a drop-in replacement for common `pip`, `pip-tools`, and `virtualenv` commands. These\ncommands work directly with the virtual environment, in contrast to uv's primary interfaces where\nthe virtual environment is managed automatically. The `uv pip` interface exposes the speed and\nfunctionality of uv to power users and projects that are not ready to transition away fr"], "retrieved": []}
{"id": "q0198", "q": "How does uv handle package installs when the target directory isn’t writable, unlike pip?", "shape": "conceptual", "gold": ["pip/compatibility.md#11"], "gold_text": ["## `--user` and the `user` install scheme\n\nuv does not support the `--user` flag, which installs packages based on the `user` install scheme.\nInstead, we recommend the use of virtual environments to isolate package installations.\n\nAdditionally, pip will fall back to the `user` install scheme if it detects that the user does not\nhave write permissions to the target directory, as is the case on some"], "retrieved": []}
{"id": "q0199", "q": "What does the `--user` flag do in uv?", "shape": "exact_lookup", "gold": ["pip/compatibility.md#11"], "gold_text": ["## `--user` and the `user` install scheme\n\nuv does not support the `--user` flag, which installs packages based on the `user` install scheme.\nInstead, we recommend the use of virtual environments to isolate package installations.\n\nAdditionally, pip will fall back to the `user` install scheme if it detects that the user does not\nhave write permissions to the target directory, as is the case on some"], "retrieved": []}
{"id": "q0200", "q": "How can uv be told to ignore project source overrides when resolving dependencies?", "shape": "conceptual", "gold": ["concepts/projects/dependencies.md#16"], "gold_text": ["### Disabling sources\n\nTo instruct uv to ignore the `tool.uv.sources` table (e.g., to simulate resolving with the package's\npublished metadata), use the `--no-sources` flag:\n\n```console\n$ uv lock --no-sources\n```\n\nThe use of `--no-sources` will also prevent uv from discovering any\n[workspace members](#workspace-member) that could satisfy a given dependency."], "retrieved": ["concepts/projects/dependencies.md#1"]}
{"id": "q0201", "q": "What does the `--no-sources` flag do in uv?", "shape": "exact_lookup", "gold": ["concepts/projects/dependencies.md#16"], "gold_text": ["### Disabling sources\n\nTo instruct uv to ignore the `tool.uv.sources` table (e.g., to simulate resolving with the package's\npublished metadata), use the `--no-sources` flag:\n\n```console\n$ uv lock --no-sources\n```\n\nThe use of `--no-sources` will also prevent uv from discovering any\n[workspace members](#workspace-member) that could satisfy a given dependency."], "retrieved": []}
{"id": "q0202", "q": "How can multiple related executables be installed together in one tool environment?", "shape": "conceptual", "gold": ["guides/tools.md#8"], "gold_text": ["Multiple related executables can be installed together in the same tool environment, using the\n`--with-executables-from` flag. For example, the following will install the executables from\n`ansible`, plus those ones provided by `ansible-core` and `ansible-lint`:\n\n```console\n$ uv tool install --with-executables-from ansible-core,ansible-lint ansible\n```"], "retrieved": ["concepts/tools.md#1"]}
{"id": "q0204", "q": "How do you keep a project's dependencies in sync with the environment?", "shape": "conceptual", "gold": ["getting-started/features.md#3"], "gold_text": ["## Projects\n\nCreating and working on Python projects, i.e., with a `pyproject.toml`.\n\n- `uv init`: Create a new Python project.\n- `uv add`: Add a dependency to the project.\n- `uv remove`: Remove a dependency from the project.\n- `uv sync`: Sync the project's dependencies with the environment.\n- `uv lock`: Create a lockfile for the project's dependencies.\n- `uv run`: Run a command in the project env"], "retrieved": []}
{"id": "q0205", "q": "What does `uv lock` do for a Python project?", "shape": "exact_lookup", "gold": ["getting-started/features.md#3"], "gold_text": ["## Projects\n\nCreating and working on Python projects, i.e., with a `pyproject.toml`.\n\n- `uv init`: Create a new Python project.\n- `uv add`: Add a dependency to the project.\n- `uv remove`: Remove a dependency from the project.\n- `uv sync`: Sync the project's dependencies with the environment.\n- `uv lock`: Create a lockfile for the project's dependencies.\n- `uv run`: Run a command in the project env"], "retrieved": []}
{"id": "q0206", "q": "When should you choose hatchling instead of the uv build backend?", "shape": "conceptual", "gold": ["concepts/build-backend.md#1"], "gold_text": ["## Choosing a build backend\n\nThe uv build backend is a great choice for most Python projects. It has reasonable defaults, with\nthe goal of requiring zero configuration for most users, but provides flexible configuration to\naccommodate most Python project structures. It integrates tightly with uv, to improve messaging and\nuser experience. It validates project metadata and structures, preventing com"], "retrieved": []}
{"id": "q0208", "q": "Why does uv use a lockfile instead of only relying on pyproject.toml?", "shape": "conceptual", "gold": ["concepts/projects/layout.md#3"], "gold_text": ["## The lockfile\n\nuv creates a `uv.lock` file next to the `pyproject.toml`.\n\n`uv.lock` is a _universal_ or _cross-platform_ lockfile that captures the packages that would be\ninstalled across all possible Python markers such as operating system, architecture, and Python\nversion.\n\nUnlike the `pyproject.toml`, which is used to specify the broad requirements of your project, the\nlockfile contains the e"], "retrieved": []}
{"id": "q0209", "q": "How does uv represent local path and Git dependencies after importing them from requirements files?", "shape": "conceptual", "gold": ["guides/migration/pip-to-project.md#12"], "gold_text": ["#### Importing dependency sources\n\nWhen importing requirements on local paths or Git repositories, for example:\n\n```requirements title=\"requirements.in\"\n./path-dep\n-e ./editable-path-dep\ngit-dep @ git+https://github.com/astral-sh/git-dep\n```\n\nuv will map them to [dependency sources](../../concepts/projects/dependencies.md#dependency-sources)\nin the `[tool.uv.sources]` table of the `pyproject.toml`"], "retrieved": []}
{"id": "q0210", "q": "What is the `[tool.uv.sources]` table used for in `pyproject.toml`?", "shape": "exact_lookup", "gold": ["guides/migration/pip-to-project.md#12"], "gold_text": ["#### Importing dependency sources\n\nWhen importing requirements on local paths or Git repositories, for example:\n\n```requirements title=\"requirements.in\"\n./path-dep\n-e ./editable-path-dep\ngit-dep @ git+https://github.com/astral-sh/git-dep\n```\n\nuv will map them to [dependency sources](../../concepts/projects/dependencies.md#dependency-sources)\nin the `[tool.uv.sources]` table of the `pyproject.toml`"], "retrieved": []}
{"id": "q0211", "q": "Why does a library project include a src layout and py.typed marker?", "shape": "conceptual", "gold": ["concepts/projects/init.md#3"], "gold_text": ["## Libraries\n\nA library provides functions and objects for other projects to consume. Libraries are intended to be\nbuilt and distributed, e.g., by uploading them to PyPI.\n\nLibraries can be created by using the `--lib` flag:\n\n```console\n$ uv init --lib example-lib\n```\n\n!!! note\n\nLibraries always require a packaged project.\n\nA `py.typed` marker is included to indicate to consumers that types can be "], "retrieved": []}
{"id": "q0212", "q": "What does the `--lib` flag do in uv init?", "shape": "exact_lookup", "gold": ["concepts/projects/init.md#3"], "gold_text": ["## Libraries\n\nA library provides functions and objects for other projects to consume. Libraries are intended to be\nbuilt and distributed, e.g., by uploading them to PyPI.\n\nLibraries can be created by using the `--lib` flag:\n\n```console\n$ uv init --lib example-lib\n```\n\n!!! note\n\nLibraries always require a packaged project.\n\nA `py.typed` marker is included to indicate to consumers that types can be "], "retrieved": ["concepts/projects/init.md#1"]}
{"id": "q0213", "q": "When should you install a tool instead of running it with uvx?", "shape": "conceptual", "gold": ["concepts/tools.md#2"], "gold_text": ["## Execution vs installation\n\nIn most cases, executing a tool with `uvx` is more appropriate than installing the tool. Installing\nthe tool is useful if you need the tool to be available to other programs on your system, e.g., if\nsome script you do not control requires the tool, or if you are in a Docker image and want to make\nthe tool available to users."], "retrieved": []}
{"id": "q0214", "q": "How should a shared Python namespace package be configured when modules live under a common dotted module path?", "shape": "conceptual", "gold": ["concepts/build-backend.md#5"], "gold_text": ["## Namespace packages\n\nNamespace packages are intended for use-cases where multiple packages write modules into a shared\nnamespace.\n\nNamespace package modules are identified by a `.` in the `module-name`. For example, to package the\nmodule `bar` in the shared namespace `foo`, the project structure would be:\n\n```text\npyproject.toml\nsrc\n└── foo\n    └── bar\n        └── __init__.py\n```\n\nAnd the `modul"], "retrieved": []}
{"id": "q0215", "q": "What does the `namespace = true` option do in uv build-backend configuration?", "shape": "exact_lookup", "gold": ["concepts/build-backend.md#5"], "gold_text": ["## Namespace packages\n\nNamespace packages are intended for use-cases where multiple packages write modules into a shared\nnamespace.\n\nNamespace package modules are identified by a `.` in the `module-name`. For example, to package the\nmodule `bar` in the shared namespace `foo`, the project structure would be:\n\n```text\npyproject.toml\nsrc\n└── foo\n    └── bar\n        └── __init__.py\n```\n\nAnd the `modul"], "retrieved": []}
{"id": "q0216", "q": "What package index does uv use by default for dependency resolution and installation?", "shape": "conceptual", "gold": ["concepts/indexes.md#0"], "gold_text": ["# Package indexes\n\nBy default, uv uses the [Python Package Index (PyPI)](https://pypi.org) for dependency resolution\nand package installation. However, uv can be configured to use other package indexes, including\nprivate indexes, via the `[[tool.uv.index]]` configuration option (and `--index`, the analogous\ncommand-line option)."], "retrieved": ["concepts/indexes.md#1"]}
{"id": "q0217", "q": "What does `--index` do in uv?", "shape": "exact_lookup", "gold": ["concepts/indexes.md#0"], "gold_text": ["# Package indexes\n\nBy default, uv uses the [Python Package Index (PyPI)](https://pypi.org) for dependency resolution\nand package installation. However, uv can be configured to use other package indexes, including\nprivate indexes, via the `[[tool.uv.index]]` configuration option (and `--index`, the analogous\ncommand-line option)."], "retrieved": []}
{"id": "q0218", "q": "How does the resolver choose package versions when a package has only one version but its dependencies require another version transitively?", "shape": "conceptual", "gold": ["concepts/resolution.md#2"], "gold_text": ["## Basic examples\n\nTo help demonstrate the resolution process, consider the following dependencies:\n\n<!-- prettier-ignore -->\n- The project depends on `foo` and `bar`.\n- `foo` has one version, 1.0.0:\n    - `foo 1.0.0` depends on `lib>=1.0.0`.\n- `bar` has one version, 1.0.0:\n    - `bar 1.0.0` depends on `lib>=2.0.0`.\n- `lib` has two versions, 1.0.0 and 2.0.0. Both versions have no dependencies.\n\nIn"], "retrieved": ["reference/internals/resolver.md#1"]}
{"id": "q0219", "q": "What does `lib>=2.0.0` require?", "shape": "exact_lookup", "gold": ["concepts/resolution.md#2"], "gold_text": ["## Basic examples\n\nTo help demonstrate the resolution process, consider the following dependencies:\n\n<!-- prettier-ignore -->\n- The project depends on `foo` and `bar`.\n- `foo` has one version, 1.0.0:\n    - `foo 1.0.0` depends on `lib>=1.0.0`.\n- `bar` has one version, 1.0.0:\n    - `bar 1.0.0` depends on `lib>=2.0.0`.\n- `lib` has two versions, 1.0.0 and 2.0.0. Both versions have no dependencies.\n\nIn"], "retrieved": []}
{"id": "q0220", "q": "How are development dependencies treated when a project is published to PyPI?", "shape": "conceptual", "gold": ["concepts/projects/dependencies.md#19"], "gold_text": ["## Development dependencies\n\nUnlike optional dependencies, development dependencies are local-only and will _not_ be included in\nthe project requirements when published to PyPI or other indexes. As such, development dependencies\nare not included in the `[project]` table.\n\nDevelopment dependencies can have entries in `tool.uv.sources` the same as normal dependencies.\n\nTo add a development dependenc"], "retrieved": []}
{"id": "q0221", "q": "What does the --only-dev flag do?", "shape": "exact_lookup", "gold": ["concepts/projects/dependencies.md#19"], "gold_text": ["## Development dependencies\n\nUnlike optional dependencies, development dependencies are local-only and will _not_ be included in\nthe project requirements when published to PyPI or other indexes. As such, development dependencies\nare not included in the `[project]` table.\n\nDevelopment dependencies can have entries in `tool.uv.sources` the same as normal dependencies.\n\nTo add a development dependenc"], "retrieved": []}
